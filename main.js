/*
THIS IS A GENERATED/BUNDLED FILE BY ESBUILD-WASM
If you want to view the source, please visit the npm repository of this plugin.
*/

var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);

// src/main.ts
var main_exports = {};
__export(main_exports, {
  SimpleScriptCompleter: () => SimpleScriptCompleter,
  default: () => main_default
});
module.exports = __toCommonJS(main_exports);
var import_obsidian7 = require("obsidian");

// src/libraryManager.ts
var import_obsidian = require("obsidian");

// src/constants.ts
var CATEGORY_ALIASES = {
  // 基础类别
  "\u89D2\u8272": "characters",
  "\u4EBA\u7269": "characters",
  "\u573A\u666F": "scenes",
  "\u5730\u70B9": "scenes",
  "\u65F6\u95F4\u6BB5": "times",
  "\u65F6\u95F4": "times",
  "\u52A8\u4F5C": "actions",
  "\u884C\u4E3A": "actions",
  "\u53F0\u8BCD": "dialogues",
  "\u5BF9\u767D": "dialogues",
  // 修仙玄幻
  "\u529F\u6CD5": "skills",
  "\u6CD5\u672F": "skills",
  "\u6CD5\u5B9D": "artifacts",
  "\u6CD5\u5668": "artifacts",
  "\u62DB\u5F0F": "techniques",
  "\u6280\u80FD": "techniques",
  "\u95E8\u6D3E": "organizations",
  "\u7EC4\u7EC7": "organizations",
  "\u52BF\u529B": "organizations",
  "\u79D8\u5883": "locations",
  "\u6D1E\u5E9C": "locations",
  "\u7279\u6B8A\u5730\u70B9": "locations",
  // 影视制作：镜头语言（剧本 / 分镜场景高频，单独成类以便参与上下文过滤）
  "\u666F\u522B": "shot",
  "\u666F\u6DF1": "shot",
  "\u62CD\u6444\u65B9\u5F0F": "camera",
  "\u8FD0\u955C": "camera",
  "\u955C\u5934\u8FD0\u52A8": "camera",
  "\u6444\u5F71\u673A\u8FD0\u52A8": "camera",
  "\u62CD\u6444\u89D2\u5EA6": "angle",
  "\u673A\u4F4D\u89D2\u5EA6": "angle",
  "\u89D2\u5EA6": "angle",
  "\u526A\u8F91\u6280\u5DE7": "editing",
  "\u526A\u8F91": "editing",
  "\u8F6C\u573A\u6280\u5DE7": "editing",
  "\u5149\u5B66\u4E0E\u955C\u5934\u8D28\u611F": "optics",
  "\u955C\u5934\u8D28\u611F": "optics",
  "\u5149\u5B66": "optics",
  "\u5149\u7EBF\u4E0E\u6C1B\u56F4": "lighting",
  "\u5149\u7EBF": "lighting",
  "\u5E03\u5149": "lighting",
  "\u5149\u5F71": "lighting",
  "\u8272\u5F69\u4E0E\u5F71\u8C03": "color",
  "\u5F71\u8C03": "color",
  "\u8272\u8C03": "color",
  "\u6784\u56FE\u65B9\u5F0F": "composition",
  "\u6784\u56FE": "composition",
  "\u98CE\u683C\u53C2\u8003": "style",
  "\u7F8E\u5B66\u98CE\u683C": "style",
  "\u5BFC\u6F14\u98CE\u683C": "style",
  // 其他
  "\u5E38\u7528\u8BCD": "commonWords",
  "\u5E38\u7528\u4FEE\u9970\u8BCD": "commonWords",
  "\u4FEE\u9970\u8BCD": "commonWords",
  "\u9AD8\u9891\u8BCD": "commonWords",
  "\u4E8B\u4EF6": "events",
  "\u60C5\u8282": "events",
  "\u60C5\u611F": "emotions",
  "\u60C5\u7EEA": "emotions",
  "\u97F3\u6548": "sounds",
  "\u58F0\u97F3": "sounds",
  "\u8F6C\u573A": "transitions",
  "\u8F6C\u573A\u6548\u679C": "transitions",
  // 英语（覆盖用户实际写法：「常用单词 / 基础单词 / 日常短语」等，
  // 归为 english* 类别后，与中文共用统一的 smartMinLength 触发阈值）
  "\u82F1\u8BED\u5355\u8BCD": "englishWords",
  "\u82F1\u6587\u5355\u8BCD": "englishWords",
  "\u5E38\u7528\u5355\u8BCD": "englishWords",
  "\u57FA\u7840\u5355\u8BCD": "englishWords",
  "\u5355\u8BCD": "englishWords",
  "\u82F1\u8BED\u8BCD\u7EC4": "englishPhrases",
  "\u82F1\u6587\u8BCD\u7EC4": "englishPhrases",
  "\u5E38\u7528\u8BCD\u7EC4": "englishPhrases",
  "\u5E38\u7528\u77ED\u8BED": "englishPhrases",
  "\u65E5\u5E38\u77ED\u8BED": "englishPhrases",
  "\u77ED\u8BED": "englishPhrases",
  "\u82F1\u8BED\u4E60\u8BED": "englishIdioms",
  "\u82F1\u6587\u4E60\u8BED": "englishIdioms",
  // 英文原名（保持兼容；多词标题会被 detectCategory 转成下划线形式）
  "characters": "characters",
  "scenes": "scenes",
  "times": "times",
  "actions": "actions",
  "dialogues": "dialogues",
  "skills": "skills",
  "artifacts": "artifacts",
  "techniques": "techniques",
  "commonwords": "commonWords",
  "common_words": "commonWords",
  "organizations": "organizations",
  "locations": "locations",
  "events": "events",
  "emotions": "emotions",
  "sounds": "sounds",
  "transitions": "transitions",
  "englishwords": "englishWords",
  "english_words": "englishWords",
  "englishphrases": "englishPhrases",
  "english_phrases": "englishPhrases",
  "englishidioms": "englishIdioms",
  "english_idioms": "englishIdioms",
  // 影视制作英文原名（detectCategory 会保留括号内容，如「拍摄方式 (Camera Movement)」→
  // "拍摄方式_(camera_movement)"，括号内英文是第二条匹配线索）
  "shot": "shot",
  "shots": "shot",
  "shot_size": "shot",
  "camera": "camera",
  "camera_movement": "camera",
  "angle": "angle",
  "camera_angle": "angle",
  "editing": "editing",
  "editing_techniques": "editing",
  "optics": "optics",
  "optics_&_lens_look": "optics",
  "lens": "optics",
  "lighting": "lighting",
  "lighting_&_atmosphere": "lighting",
  "color": "color",
  "color_&_tone": "color",
  "composition": "composition",
  "style": "style",
  "style_references": "style",
  "common_modifiers": "commonWords"
};
var CAMERA_LANGUAGE_CATS = [
  "shot",
  "camera",
  "angle",
  "editing",
  "optics",
  "lighting",
  "color",
  "composition",
  "style"
];
var CONTEXT_CATEGORY_MAP = {
  // scene_time 旧映射已删除：detectLineContext 从不产出 'scene_time'，属于死映射
  scene_location: ["scene", "scene_time_combo", "location", ...CAMERA_LANGUAGE_CATS],
  character_name: ["character"],
  action: ["action", "technique", "skill", ...CAMERA_LANGUAGE_CATS],
  // general 不再用 'all' 全放行（否则正文行下上下文感知形同虚设）；
  // 改为宽泛但仍有边界的内容类别，组合类(scene_time_combo/dialogue_combo)与专用类(skill/technique/artifact)不在此列。
  general: [
    "character",
    "scene",
    "time",
    "action",
    "dialogue",
    "commonWords",
    "organization",
    "location",
    "event",
    "emotion",
    "sound",
    "transition",
    "englishWord",
    "englishPhrase",
    "englishIdiom",
    ...CAMERA_LANGUAGE_CATS
  ]
};
var CATEGORY_TYPE_MAP = {
  characters: "character",
  scenes: "scene",
  times: "time",
  actions: "action",
  dialogues: "dialogue",
  skills: "skill",
  artifacts: "artifact",
  techniques: "technique",
  commonWords: "commonWords",
  organizations: "organization",
  locations: "location",
  events: "event",
  emotions: "emotion",
  sounds: "sound",
  transitions: "transition",
  englishWords: "englishWord",
  englishPhrases: "englishPhrase",
  englishIdioms: "englishIdiom",
  // 影视制作
  shot: "shot",
  camera: "camera",
  angle: "angle",
  editing: "editing",
  optics: "optics",
  lighting: "lighting",
  color: "color",
  composition: "composition",
  style: "style"
};
var CATEGORY_DISPLAY_MAP = {
  characters: "\u89D2\u8272",
  scenes: "\u573A\u666F",
  times: "\u65F6\u95F4\u6BB5",
  actions: "\u52A8\u4F5C",
  dialogues: "\u53F0\u8BCD",
  skills: "\u529F\u6CD5",
  artifacts: "\u6CD5\u5B9D",
  techniques: "\u62DB\u5F0F",
  commonWords: "\u5E38\u7528\u8BCD",
  organizations: "\u7EC4\u7EC7",
  locations: "\u7279\u6B8A\u5730\u70B9",
  events: "\u4E8B\u4EF6",
  emotions: "\u60C5\u611F",
  sounds: "\u97F3\u6548",
  transitions: "\u8F6C\u573A",
  englishWords: "\u82F1\u8BED\u5355\u8BCD",
  englishPhrases: "\u82F1\u8BED\u8BCD\u7EC4",
  englishIdioms: "\u82F1\u8BED\u4E60\u8BED",
  character: "\u89D2\u8272",
  scene: "\u573A\u666F",
  time: "\u65F6\u95F4\u6BB5",
  action: "\u52A8\u4F5C",
  dialogue: "\u53F0\u8BCD",
  skill: "\u529F\u6CD5",
  artifact: "\u6CD5\u5B9D",
  technique: "\u62DB\u5F0F",
  organization: "\u7EC4\u7EC7",
  location: "\u7279\u6B8A\u5730\u70B9",
  event: "\u4E8B\u4EF6",
  emotion: "\u60C5\u611F",
  sound: "\u97F3\u6548",
  transition: "\u8F6C\u573A",
  englishWord: "\u82F1\u8BED\u5355\u8BCD",
  englishPhrase: "\u82F1\u8BED\u8BCD\u7EC4",
  englishIdiom: "\u82F1\u8BED\u4E60\u8BED",
  scene_time_combo: "\u573A\u666F+\u65F6\u95F4",
  dialogue_combo: "\u89D2\u8272\u5BF9\u8BDD",
  shot: "\u666F\u522B",
  camera: "\u62CD\u6444\u65B9\u5F0F",
  angle: "\u62CD\u6444\u89D2\u5EA6",
  editing: "\u526A\u8F91\u6280\u5DE7",
  optics: "\u955C\u5934\u8D28\u611F",
  lighting: "\u5149\u7EBF\u6C1B\u56F4",
  color: "\u8272\u5F69\u5F71\u8C03",
  composition: "\u6784\u56FE",
  style: "\u98CE\u683C"
};
var TYPE_ORDER = {
  character: 1,
  scene: 2,
  time: 3,
  action: 4,
  dialogue: 5,
  skill: 6,
  artifact: 7,
  technique: 8,
  scene_time_combo: 9,
  dialogue_combo: 10,
  commonWords: 11,
  englishWord: 12,
  englishPhrase: 13,
  englishIdiom: 14,
  organization: 15,
  location: 16,
  event: 17,
  emotion: 18,
  sound: 19,
  transition: 20,
  // 影视制作类：排在内容类之后、兜底类之前
  shot: 21,
  camera: 22,
  angle: 23,
  composition: 24,
  lighting: 25,
  color: 26,
  optics: 27,
  editing: 28,
  style: 29
};
var DEFAULT_SETTINGS = {
  enabled: true,
  autoDialogue: true,
  enableSmartCompletion: true,
  enableContextAware: true,
  enableMinimalTrigger: true,
  enablePinyin: true,
  smartMinLength: 1,
  smartMaxSuggestions: 10,
  defaultSceneType: "int",
  enableQuickPanel: true,
  enableAutoRefresh: true,
  showAutoRefreshNotice: false,
  floatingButtonPosition: { x: 20, y: 60 },
  libraryColors: {},
  libraryIcons: {},
  libraryFolder: "",
  usageStats: {},
  enableCombos: true,
  comboMaxItems: 60
};
var DEFAULT_LIBRARY_PALETTE = [
  "#f59e0b",
  "#3b82f6",
  "#10b981",
  "#ec4899",
  "#8b5cf6",
  "#06b6d4"
];
var WORD_BOUNDARY_RE = /[\s.,;:!?\u3002\uff0c\uff1b\uff1a\uff01\uff1f\u3001\u2026\u2014\u00b7\uff08\uff09()[\]{}\u3010\u3011\u300c\u300d\u300e\u300f\u201c\u201d\u2018\u2019\u300a\u300b]/;
var MAX_COMPLETION_QUERY_LEN = 6;
var SYSTEM_CATEGORY_SET = /* @__PURE__ */ new Set([
  "character",
  "scene",
  "time",
  "action",
  "dialogue",
  "skill",
  "artifact",
  "technique",
  "commonWords",
  "organization",
  "location",
  "event",
  "emotion",
  "sound",
  "transition",
  "englishWord",
  "englishPhrase",
  "englishIdiom",
  "scene_time_combo",
  "dialogue_combo",
  // 影视制作
  "shot",
  "camera",
  "angle",
  "editing",
  "optics",
  "lighting",
  "color",
  "composition",
  "style"
]);
var PINYIN_BRANCH = [
  ["a", "\u554A"],
  ["b", "\u516B"],
  ["c", "\u64E6"],
  ["d", "\u642D"],
  ["e", "\u86FE"],
  ["f", "\u53D1"],
  ["g", "\u5676"],
  ["h", "\u54C8"],
  ["j", "\u51FB"],
  ["k", "\u5580"],
  ["l", "\u5783"],
  ["m", "\u5988"],
  ["n", "\u62FF"],
  ["o", "\u54E6"],
  ["p", "\u556A"],
  ["q", "\u671F"],
  ["r", "\u7136"],
  ["s", "\u6492"],
  ["t", "\u584C"],
  ["w", "\u6316"],
  ["x", "\u6614"],
  ["y", "\u538B"],
  ["z", "\u531D"]
];

// src/libraryManager.ts
var LibraryManager = class {
  constructor(plugin) {
    this.plugin = plugin;
    this.libraries = /* @__PURE__ */ new Map();
    this.activeLibrary = null;
    this.activeLibraryData = null;
    this.app = plugin.app;
  }
  async initialize() {
    await this.loadLibraries();
  }
  getLibraryDirectory() {
    return this.plugin.settings.libraryFolder || "";
  }
  async loadLibraries() {
    this.libraries.clear();
    const libraryDir = this.getLibraryDirectory();
    if (!libraryDir) return;
    try {
      const folder = this.app.vault.getAbstractFileByPath(libraryDir);
      if (!folder) return;
      if (!(folder instanceof import_obsidian.TFile) && !(folder instanceof import_obsidian.TFolder)) return;
      const files = [];
      this.collectMdFiles(folder, files);
      let successCount = 0;
      for (const file of files) {
        if (file instanceof import_obsidian.TFile && file.extension === "md") {
          if (await this.loadLibraryFile(file)) successCount++;
        }
      }
    } catch (error) {
      console.error("\u52A0\u8F7D\u8BCD\u5E93\u5931\u8D25:", error);
    }
  }
  collectMdFiles(folder, result) {
    if (folder instanceof import_obsidian.TFile) {
      if (folder.extension === "md") result.push(folder);
      return;
    }
    for (const child of folder.children) {
      if (child instanceof import_obsidian.TFile) {
        if (child.extension === "md") result.push(child);
      } else if (child instanceof import_obsidian.TFolder) {
        this.collectMdFiles(child, result);
      }
    }
  }
  async loadLibraryFile(file) {
    try {
      const content = await this.app.vault.read(file);
      const libraryName = file.basename;
      const libraryData = this.parseLibraryContent(content, libraryName);
      this.libraries.set(libraryName, {
        name: libraryName,
        path: file.path,
        data: libraryData,
        lastModified: file.stat.mtime
      });
      return true;
    } catch (error) {
      console.error(`\u52A0\u8F7D\u8BCD\u5E93\u6587\u4EF6\u5931\u8D25: ${file.path}`, error);
      return false;
    }
  }
  parseLibraryContent(content, libraryName) {
    var _a, _b;
    const data = { name: libraryName, metadata: {} };
    let currentSection = null;
    let parentSection = null;
    const lines = content.split("\n");
    if (lines[0] && lines[0].trim() === "---") {
      for (let i = 1; i < lines.length; i++) {
        if (lines[i].trim() === "---") break;
        const parts = lines[i].split(":").map((p) => p.trim());
        if (parts.length >= 2) {
          data.metadata[parts[0]] = parts.slice(1).join(":");
        }
      }
    }
    for (let i = 0; i < lines.length; i++) {
      const trimmedLine = lines[i].trim();
      if (trimmedLine.startsWith("#")) {
        const level = (_b = (_a = trimmedLine.match(/^#+/)) == null ? void 0 : _a[0].length) != null ? _b : 1;
        if (level <= 2) {
          currentSection = this.detectCategory(trimmedLine);
          parentSection = currentSection;
        } else {
          currentSection = parentSection;
        }
        if (currentSection && !data[currentSection]) {
          data[currentSection] = [];
        }
      } else if (currentSection && trimmedLine && !trimmedLine.startsWith("#")) {
        const item = this.parseLibraryItem(trimmedLine);
        if (item) {
          if (!data[currentSection]) {
            data[currentSection] = [];
          }
          data[currentSection].push(item);
        }
      }
    }
    return data;
  }
  /**
   * 章节标题 → 类别键（中英归一化为规范英文键，见 CATEGORY_ALIASES）。
   *
   * 带英文括号的标题（如「拍摄方式 (Camera Movement)」）会产生三种候选键，按序查表：
   *   1. 完整标题    → "拍摄方式_(camera_movement)"
   *   2. 去括号中文主干 → "拍摄方式"
   *   3. 括号内英文原名 → "camera_movement"
   * 只要任一命中别名表即归一；全不命中才原样作为自定义类别键返回。
   */
  detectCategory(line) {
    const cleanLine = line.replace(/^#+\s*/, "").trim();
    if (!cleanLine) return null;
    const toKey = (s) => s.toLowerCase().replace(/\s+/g, "_");
    const candidates = [toKey(cleanLine)];
    const withoutParens = cleanLine.replace(/[（(][^）)]*[）)]/g, "").trim();
    if (withoutParens && withoutParens !== cleanLine) candidates.push(toKey(withoutParens));
    const parens = cleanLine.match(/[（(]([^）)]*)[）)]/);
    if (parens && parens[1].trim()) candidates.push(toKey(parens[1].trim()));
    for (const key of candidates) {
      const hit = CATEGORY_ALIASES[key];
      if (hit) return hit;
    }
    return candidates[0];
  }
  /** 解析词库条目：支持 显示|插入|描述 / 显示|插入 / 显示 三种格式 */
  parseLibraryItem(line) {
    const cleanLine = line.replace(/^[-*]\s*/, "").trim();
    if (!cleanLine) return null;
    const parts = cleanLine.split("|").map((part) => part.trim());
    if (parts.length >= 3) {
      return { display: parts[0], insert: parts[1], description: parts[2] };
    } else if (parts.length === 2) {
      return { display: parts[0], insert: parts[1] };
    } else {
      return { display: parts[0], insert: parts[0] };
    }
  }
  async reloadLibraries() {
    await this.loadLibraries();
    if (this.activeLibrary && !this.libraries.has(this.activeLibrary)) {
      this.activeLibrary = null;
      this.activeLibraryData = null;
    }
  }
  /** 词库名规范化：只保留中英文与数字。
   * 历史配置里可能出现 emoji 前缀（如 "🅰单词"），而词库名取自文件 basename（"单词"），
   * 精确比较会静默失败——规范化后即可容忍 emoji / 空格 / 大小写差异。
   */
  normalizeLibraryKey(name) {
    return name.toLowerCase().replace(/[^a-z0-9\u4e00-\u9fff]/g, "");
  }
  /** 解析词库名：精确命中优先，其次规范化匹配；都不中返回 null */
  resolveLibraryName(libraryName) {
    if (!libraryName) return null;
    if (this.libraries.has(libraryName)) return libraryName;
    const target = this.normalizeLibraryKey(libraryName);
    if (!target) return null;
    for (const name of this.libraries.keys()) {
      if (this.normalizeLibraryKey(name) === target) return name;
    }
    return null;
  }
  async setActiveLibrary(libraryName) {
    if (!libraryName) return false;
    const resolved = this.resolveLibraryName(libraryName);
    if (!resolved) return false;
    this.activeLibrary = resolved;
    this.activeLibraryData = this.libraries.get(resolved).data;
    return true;
  }
  getActiveLibraryData() {
    return this.activeLibraryData;
  }
  getAvailableLibraries() {
    return Array.from(this.libraries.keys());
  }
  getLibraryInfo(libraryName) {
    const library = this.libraries.get(libraryName);
    if (!library) return null;
    const data = library.data;
    let itemCount = 0;
    const categoryCounts = {};
    for (const [category, items] of Object.entries(data)) {
      if (category !== "name" && category !== "metadata" && Array.isArray(items)) {
        const count = items.length;
        itemCount += count;
        categoryCounts[category] = count;
      }
    }
    return {
      name: library.name,
      path: library.path,
      itemCount,
      categoryCounts,
      metadata: data.metadata || {}
    };
  }
};

// src/libraryModals.ts
var import_obsidian2 = require("obsidian");
var LibraryCreationModal = class extends import_obsidian2.Modal {
  constructor(app, callback) {
    super(app);
    this.callback = callback;
  }
  onOpen() {
    const { contentEl } = this;
    contentEl.empty();
    contentEl.addClass("blfc-plugin");
    contentEl.createEl("h3", { text: "\u521B\u5EFA\u65B0\u8BCD\u5E93" });
    const inputEl = contentEl.createEl("input", {
      type: "text",
      placeholder: "\u8BF7\u8F93\u5165\u8BCD\u5E93\u540D\u79F0",
      cls: "blfc-lib-name-input"
    });
    setTimeout(() => inputEl.focus(), 100);
    const buttonContainer = contentEl.createDiv({ cls: "blfc-modal-btn-container" });
    const cancelButton = buttonContainer.createEl("button", { text: "\u53D6\u6D88" });
    cancelButton.onclick = () => this.close();
    const confirmButton = buttonContainer.createEl("button", { text: "\u521B\u5EFA" });
    confirmButton.onclick = () => this.submit(inputEl.value);
    inputEl.addEventListener("keydown", (e) => {
      if (e.key === "Enter") this.submit(inputEl.value);
    });
  }
  submit(raw) {
    const libraryName = raw.trim();
    if (libraryName) this.callback(libraryName);
    this.close();
  }
  onClose() {
    this.contentEl.empty();
  }
};
var LibrarySwitcherModal = class extends import_obsidian2.Modal {
  constructor(app, libraries, currentLibrary, callback) {
    super(app);
    this.libraries = libraries;
    this.currentLibrary = currentLibrary;
    this.callback = callback;
  }
  onOpen() {
    const { contentEl } = this;
    contentEl.empty();
    contentEl.addClass("blfc-plugin");
    contentEl.createEl("h3", { text: "\u5207\u6362\u8BCD\u5E93" });
    if (this.libraries.length === 0) {
      contentEl.createEl("p", { text: "\u6CA1\u6709\u627E\u5230\u8BCD\u5E93\u6587\u4EF6\u3002\u8BF7\u5728\u8BBE\u7F6E\u4E2D\u6307\u5B9A\u8BCD\u5E93\u6587\u4EF6\u5939\u3002" });
      return;
    }
    const list = contentEl.createDiv({ cls: "blfc-lib-list" });
    this.libraries.forEach((library) => {
      const item = list.createDiv({ cls: "blfc-lib-item" });
      if (library === this.currentLibrary) {
        item.addClass("blfc-is-active");
      }
      item.createDiv({ text: library, cls: "blfc-lib-name" });
      item.onclick = () => {
        this.callback(library);
        this.close();
      };
    });
    const buttonContainer = contentEl.createDiv({ cls: "blfc-modal-btn-container" });
    const cancelButton = buttonContainer.createEl("button", { text: "\u53D6\u6D88" });
    cancelButton.onclick = () => this.close();
  }
  onClose() {
    this.contentEl.empty();
  }
};

// src/quickPanel.ts
var import_obsidian3 = require("obsidian");

// src/dom.ts
function setCssVar(el, name, value) {
  if (typeof el.setCssProps === "function") {
    el.setCssProps({ [name]: value });
  } else {
    el.style.setProperty(name, value);
  }
}

// src/quickPanel.ts
var ICON_NAME_RE = /^[a-z0-9][a-z0-9-]*$/i;
function sanitizeSvg(raw) {
  const doc = new DOMParser().parseFromString(raw, "text/html");
  const holder = doc.body;
  holder.querySelectorAll("script").forEach((el) => el.remove());
  holder.querySelectorAll("*").forEach((el) => {
    Array.from(el.attributes).forEach((attr) => {
      const n = attr.name.toLowerCase();
      const v = attr.value.trim().toLowerCase();
      if (n.startsWith("on") || (n === "href" || n === "xlink:href") && v.startsWith("javascript:")) {
        el.removeAttribute(attr.name);
      }
    });
  });
  return holder.innerHTML;
}
var LibraryEdgeStrips = class {
  constructor(plugin) {
    this.plugin = plugin;
    this.container = null;
    /** CI-* 等第三方插件图标可能晚于本插件注册：未解析成功时递增重试轮数 */
    this.retryAttempt = 0;
    this.retryTimer = null;
    /** 观察编辑器根分栏尺寸变化（侧边栏展开/收起等），驱动位置重算 */
    this.resizeObserver = null;
    /** rAF 合并标记：过渡动画期间 RO 高频回调，只保留一帧一次对齐 */
    this.rafPending = false;
    /** 当前被 RO 观察的元素（避免 layout-change 时重复重建观察器） */
    this.observedEl = null;
    this.layoutHandler = () => {
      this.observeRoot();
      this.scheduleReposition();
    };
  }
  /** 创建彩条容器并首次渲染 */
  create() {
    if (this.container && this.container.parentNode) {
      this.container.remove();
    }
    this.container = document.body.createDiv();
    this.container.className = "blfc-edge-strips";
    document.body.appendChild(this.container);
    this.plugin.app.workspace.on("layout-change", this.layoutHandler);
    window.addEventListener("resize", this.layoutHandler);
    this.observeRoot();
    this.render();
  }
  /** 销毁容器与监听 */
  destroy() {
    if (this.retryTimer != null) {
      window.clearTimeout(this.retryTimer);
      this.retryTimer = null;
    }
    if (this.resizeObserver) {
      this.resizeObserver.disconnect();
      this.resizeObserver = null;
      this.observedEl = null;
    }
    this.plugin.app.workspace.off("layout-change", this.layoutHandler);
    window.removeEventListener("resize", this.layoutHandler);
    if (this.container && this.container.parentNode) {
      this.container.remove();
    }
    this.container = null;
    this.retryAttempt = 0;
  }
  /**
   * 重新渲染彩条（词库列表/当前词库/颜色变化后调用）。
   * 同时负责空态隐藏与位置对齐。
   */
  refresh() {
    this.render();
  }
  /**
   * 原位切换激活条（不重建 DOM）：让激活图标的 scale 放大过渡真实可见。
   * 遍历现有 .blfc-edge-strip，把激活类移到目标词库；仅供点击切换路径在 render() 前调用。
   */
  activateStrip(libraryName) {
    if (!this.container) return;
    this.container.querySelectorAll(".blfc-edge-strip").forEach((el) => {
      const strip = el;
      strip.classList.toggle("blfc-edge-strip-active", strip.getAttribute("data-library") === libraryName);
    });
  }
  /** 渲染全部彩条 */
  render() {
    if (!this.container) return;
    const libraries = this.plugin.libraryManager.getAvailableLibraries();
    this.container.empty();
    if (libraries.length === 0) {
      this.container.addClass("blfc-hidden");
      return;
    }
    this.container.removeClass("blfc-hidden");
    const activeLibrary = this.plugin.libraryManager.activeLibrary;
    const colors = this.plugin.settings.libraryColors;
    const pendingNames = [];
    libraries.forEach((libraryName, index) => {
      var _a, _b;
      let color = colors[libraryName];
      if (!color) {
        color = DEFAULT_LIBRARY_PALETTE[index % DEFAULT_LIBRARY_PALETTE.length];
        this.plugin.settings.libraryColors[libraryName] = color;
        void this.plugin.saveSettings();
      }
      const strip = this.container.createDiv();
      strip.className = "blfc-edge-strip";
      setCssVar(strip, "--blfc-strip-bg", color);
      if (libraryName === activeLibrary) {
        strip.classList.add("blfc-edge-strip-active");
      }
      const rawIcon = ((_b = (_a = this.plugin.settings.libraryIcons) == null ? void 0 : _a[libraryName]) != null ? _b : "").trim();
      if (rawIcon) {
        const iconEl = strip.createSpan();
        iconEl.className = "blfc-edge-strip-icon";
        let iconOk = false;
        if (rawIcon.toLowerCase().startsWith("<svg")) {
          const parsed = new DOMParser().parseFromString(sanitizeSvg(rawIcon), "text/html");
          Array.from(parsed.body.childNodes).forEach((n) => iconEl.appendChild(n));
          iconOk = true;
        } else if (ICON_NAME_RE.test(rawIcon)) {
          try {
            (0, import_obsidian3.setIcon)(iconEl, rawIcon);
          } catch (e) {
          }
          iconOk = !!iconEl.querySelector("svg");
        } else {
          iconEl.textContent = rawIcon;
          iconOk = true;
        }
        if (iconOk) {
          strip.classList.add("blfc-icon-only");
        } else {
          iconEl.remove();
          pendingNames.push(`${libraryName} \u2192 ${rawIcon}`);
        }
      }
      const info = this.plugin.libraryManager.getLibraryInfo(libraryName);
      const count = info ? info.itemCount : 0;
      strip.title = `${libraryName}\uFF08${count} \u6761\uFF09`;
      strip.setAttribute("data-library", libraryName);
      strip.setAttribute("role", "button");
      strip.setAttribute("aria-label", `\u5207\u6362\u5230\u8BCD\u5E93 ${libraryName}`);
      strip.tabIndex = 0;
      const activate = async () => {
        if (libraryName === this.plugin.libraryManager.activeLibrary) return;
        const ok = await this.plugin.libraryManager.setActiveLibrary(libraryName);
        if (!ok) return;
        await this.plugin.buildSmartCompletionIndex();
        this.plugin.updateStatusBar();
        this.activateStrip(libraryName);
        window.setTimeout(() => {
          if (this.container) this.render();
        }, 180);
        new import_obsidian3.Notice(`\u5DF2\u5207\u6362\u5230\u8BCD\u5E93: ${libraryName}`);
      };
      strip.addEventListener("click", () => {
        void activate();
      });
      strip.addEventListener("keydown", (e) => {
        if (e.key === "Enter" || e.key === " ") {
          e.preventDefault();
          void activate();
        }
      });
      this.container.appendChild(strip);
    });
    this.reposition();
    if (pendingNames.length > 0) {
      this.scheduleIconRetry(pendingNames);
    } else {
      this.retryAttempt = 0;
    }
  }
  /**
   * 图标延时重试：CI-* 等第三方插件图标注册时机不定（插件加载顺序/懒注册），
   * 在 0.4s / 1.2s / 3s / 8s 后各重试一次；仍失败则打日志（大概率来源插件未启用）。
   */
  scheduleIconRetry(pendingNames) {
    if (this.retryTimer != null) return;
    const delays = [400, 1200, 3e3, 8e3];
    const delay = delays[this.retryAttempt];
    if (delay === void 0) {
      console.warn(
        "[InFlow] \u56FE\u6807\u591A\u6B21\u91CD\u8BD5\u4ECD\u65E0\u6CD5\u89E3\u6790\uFF08\u8BF7\u786E\u8BA4\u6765\u6E90\u63D2\u4EF6\u5DF2\u542F\u7528\u4E14\u665A\u4E8E\u672C\u63D2\u4EF6\u52A0\u8F7D\uFF1B\u6216\u6539\u7528 lucide \u5185\u7F6E\u56FE\u6807\u540D / Emoji\uFF09\uFF1A",
        pendingNames
      );
      this.retryAttempt = 0;
      return;
    }
    this.retryAttempt++;
    this.retryTimer = window.setTimeout(() => {
      this.retryTimer = null;
      if (this.container) this.render();
    }, delay);
  }
  /**
   * 获取定位参考元素：编辑器根分栏（rootSplit）容器。
   * rootSplit 在部分 Obsidian 类型定义里未暴露 containerEl，这里做宽松断言。
   */
  getRootElement() {
    const root = this.plugin.app.workspace.rootSplit;
    const rootEl = root == null ? void 0 : root.containerEl;
    if (rootEl) return rootEl;
    return document.querySelector(".workspace-split.mod-root");
  }
  /**
   * 观察编辑器根分栏：侧边栏展开/收起（不一定触发 layout-change / resize）会改变其
   * 位置或尺寸，ResizeObserver 均能捕获，从而让彩条实时跟随对齐。
   */
  observeRoot() {
    if (typeof ResizeObserver === "undefined") return;
    const target = this.getRootElement();
    if (!target || target === this.observedEl) return;
    if (this.resizeObserver) {
      this.resizeObserver.disconnect();
      this.resizeObserver = null;
    }
    this.observedEl = target;
    this.resizeObserver = new ResizeObserver(() => this.scheduleReposition());
    this.resizeObserver.observe(target);
  }
  /** 合并到下一帧执行对齐：过渡动画期间 RO 高频回调，避免重复 reflow */
  scheduleReposition() {
    if (this.rafPending) return;
    this.rafPending = true;
    window.requestAnimationFrame(() => {
      this.rafPending = false;
      this.reposition();
    });
  }
  /** 计算左边缘偏移：对齐到编辑器根分栏（mod-root）左侧，避开左侧边栏/ribbon */
  reposition() {
    if (!this.container) return;
    const rootEl = this.getRootElement();
    const rect = rootEl == null ? void 0 : rootEl.getBoundingClientRect();
    const left = rect ? Math.max(0, rect.left) : 0;
    setCssVar(this.container, "--blfc-strips-left", `${left}px`);
  }
};

// src/textInserter.ts
var import_view = require("@codemirror/view");
var _TextInserter = class _TextInserter {
  /**
   * 规则类模板插入后的收尾：不再追加换行，而是确保末尾留一个空格，方便接着输入内容。
   * - "## 1.1 内景."        → "## 1.1 内景. "
   * - "#### "（已有空格）    → "#### "（不重复加空格）
   * - "`[描述]`\n`[描述]`"   → 原样（自带换行的多行模板保持其结构）
   */
  static ensureTrailingSpace(text) {
    if (!text) return text;
    return /\s$/.test(text) ? text : text + " ";
  }
  /**
   * 选中补全项时，光标【前】有多少字符需要被覆盖（= 查询/触发词 + 其后连续非空白输入）。
   *
   * 传入的 prefixChar 由补全流程决定：
   * - 格式补全：触发符本身（`@` / `~`）
   * - 智能补全：实际命中的查询词，可能是多个字（如「主角」），
   *   由 SimpleScriptCompleter.resolveCompletion() 解析，与替换长度严格一致。
   *
   * - "…@" + "@"        → 1（只吃掉 @）
   * - "@@" + "@"        → 1（只吃掉最后一个 @）
   * - "主角" + "主角"    → 2（吃掉整个已输入前缀）
   * - "我说主角" + "主角" → 2（只吃掉「主角」，前面的正文不动）
   * - "@ 你好" + "@"     → 1（跨越空白即停止，绝不误删既有正文）
   * - 找不到前缀字符      → 0（退化为纯插入）
   */
  static calcReplaceLength(textBefore, prefixChar) {
    if (!prefixChar || !textBefore) return 0;
    const idx = textBefore.lastIndexOf(prefixChar);
    if (idx < 0) return 0;
    const tail = textBefore.slice(idx + prefixChar.length);
    const m = /^[^\s]*/.exec(tail);
    return prefixChar.length + (m ? m[0].length : 0);
  }
  static isEditable(el) {
    if (!el || !el.tagName) return false;
    const tag = el.tagName.toLowerCase();
    if (tag === "input") {
      const type = (el.type || "").toLowerCase();
      return type === "text" || type === "search" || type === "" || type === "url" || type === "email";
    }
    if (tag === "textarea") return true;
    if (el.isContentEditable) return true;
    if (el.closest(".cm-editor")) return true;
    return false;
  }
  static isInExcludedContainer(el) {
    if (!el) return true;
    if (el.closest(".blfc-plugin")) return true;
    if (el.closest(".modal-container")) return true;
    if (el.closest(".blfc-edge-strips")) return true;
    if (el.closest(".suggestion-container")) return true;
    if (el.closest(".notice-container")) return true;
    return false;
  }
  static getTextBeforeCursor(el) {
    const cm = _TextInserter.getCodeMirrorView(el);
    if (cm) {
      const pos = cm.state.selection.main.head;
      const line = cm.state.doc.lineAt(pos);
      return line.text.substring(0, pos - line.from);
    }
    if (el.tagName && (el.tagName.toLowerCase() === "input" || el.tagName.toLowerCase() === "textarea")) {
      const inputEl = el;
      return inputEl.value.substring(0, inputEl.selectionStart || 0);
    }
    const sel = window.getSelection();
    if (sel && sel.rangeCount > 0 && sel.anchorNode && el.contains(sel.anchorNode)) {
      const range = sel.getRangeAt(0);
      const preRange = document.createRange();
      preRange.selectNodeContents(el);
      preRange.setEnd(range.endContainer, range.endOffset);
      return preRange.toString();
    }
    return "";
  }
  static getCurrentLineText(el) {
    const cm = _TextInserter.getCodeMirrorView(el);
    if (cm) {
      const pos = cm.state.selection.main.head;
      const line = cm.state.doc.lineAt(pos);
      return line.text;
    }
    if (el.tagName && el.tagName.toLowerCase() === "textarea") {
      const text = el.value;
      const pos = el.selectionStart || 0;
      const lineStart = text.lastIndexOf("\n", pos - 1) + 1;
      const lineEnd = text.indexOf("\n", pos);
      return text.substring(lineStart, lineEnd === -1 ? text.length : lineEnd);
    }
    if (el.tagName && el.tagName.toLowerCase() === "input") {
      return el.value || "";
    }
    return "";
  }
  static getOrCreateMirror(el) {
    if (!_TextInserter._mirror) {
      _TextInserter._mirror = document.body.createEl("pre");
      _TextInserter._mirror.className = "blfc-text-mirror";
      document.body.appendChild(_TextInserter._mirror);
    }
    const mirror = _TextInserter._mirror;
    const style = window.getComputedStyle(el);
    mirror.style.font = style.font;
    mirror.style.fontSize = style.fontSize;
    mirror.style.fontFamily = style.fontFamily;
    mirror.style.lineHeight = style.lineHeight;
    mirror.style.letterSpacing = style.letterSpacing;
    const elRect = el.getBoundingClientRect();
    const paddingLeft = parseFloat(style.paddingLeft) || 0;
    const paddingRight = parseFloat(style.paddingRight) || 0;
    mirror.style.width = elRect.width - paddingLeft - paddingRight + "px";
    mirror.style.padding = style.padding || "0";
    return mirror;
  }
  static getCursorScreenPosition(el) {
    const cm = _TextInserter.getCodeMirrorView(el);
    if (cm) {
      const pos = cm.state.selection.main.head;
      const coords = cm.coordsAtPos(pos);
      if (coords) return { x: coords.left, y: coords.bottom, height: coords.bottom - coords.top };
    }
    if (el.tagName && (el.tagName.toLowerCase() === "input" || el.tagName.toLowerCase() === "textarea")) {
      const inputEl = el;
      const mirror = _TextInserter.getOrCreateMirror(el);
      const pos = inputEl.selectionStart || 0;
      const textBefore = inputEl.value.substring(0, pos);
      mirror.textContent = textBefore.replace(/\n$/, "\n\xA0");
      const rect2 = el.getBoundingClientRect();
      const mirrorRect = mirror.getBoundingClientRect();
      const style = window.getComputedStyle(el);
      const lineHeight = parseFloat(style.lineHeight) || parseFloat(style.fontSize) * 1.2 || 16;
      return {
        x: rect2.left + (mirrorRect.right - mirrorRect.left),
        y: rect2.top + (mirrorRect.bottom - mirrorRect.top),
        height: lineHeight
      };
    }
    const rect = el.getBoundingClientRect();
    return { x: rect.left, y: rect.bottom, height: 20 };
  }
  static getCodeMirrorView(el) {
    var _a;
    if (!el || typeof el.closest !== "function") return null;
    const cmEl = el.closest(".cm-editor") || (typeof el.querySelector === "function" ? el.querySelector(".cm-editor") : null);
    if (!cmEl) return null;
    try {
      const view = import_view.EditorView.findFromDOM(cmEl);
      if (view) return view;
    } catch (e) {
    }
    const marker = cmEl;
    return ((_a = marker.cmView) == null ? void 0 : _a.view) || null;
  }
  /** 将 CM6 EditorView 包装为 EditorLike 代理（用于场景编号 / 重编号等行级操作） */
  static createCMEditorProxy(cm) {
    return {
      getCursor: () => {
        const pos = cm.state.selection.main.head;
        const line = cm.state.doc.lineAt(pos);
        return { line: line.number - 1, ch: pos - line.from };
      },
      getLine: (n) => cm.state.doc.line(n + 1).text,
      lineCount: () => cm.state.doc.lines,
      setLine: (n, text) => {
        const line = cm.state.doc.line(n + 1);
        cm.dispatch({ changes: { from: line.from, to: line.to, insert: text } });
      },
      replaceRange: (text, pos) => {
        const line = cm.state.doc.line(pos.line + 1);
        const from = line.from + pos.ch;
        cm.dispatch({ changes: { from, insert: text } });
        cm.focus();
      },
      setSelection: (from, to) => {
        const lineFrom = cm.state.doc.line(from.line + 1);
        const lineTo = cm.state.doc.line(to.line + 1);
        cm.dispatch({
          selection: {
            anchor: lineFrom.from + from.ch,
            head: lineTo.from + to.ch
          }
        });
        cm.focus();
      }
    };
  }
  static insertText(el, text) {
    const cm = _TextInserter.getCodeMirrorView(el);
    if (cm) {
      const from = cm.state.selection.main.head;
      cm.dispatch({
        changes: { from, insert: text },
        selection: { anchor: from + text.length }
      });
      cm.focus();
      return;
    }
    if (el.tagName && (el.tagName.toLowerCase() === "input" || el.tagName.toLowerCase() === "textarea")) {
      const inputEl = el;
      const start = inputEl.selectionStart || 0;
      const end = inputEl.selectionEnd || start;
      inputEl.value = inputEl.value.substring(0, start) + text + inputEl.value.substring(end);
      const newPos = start + text.length;
      inputEl.selectionStart = newPos;
      inputEl.selectionEnd = newPos;
      inputEl.focus();
      inputEl.dispatchEvent(new Event("input", { bubbles: true }));
      return;
    }
    el.focus();
    document.execCommand("insertText", false, text);
  }
  /**
   * 删除光标【前】deleteBefore 个字符，再插入 text。
   * 用于选中补全项时把触发符（@ / ~ / 已输入的查询字）一并替换掉，
   * 避免触发符残留进文档（与 replaceRange 的“向后删除”方向相反）。
   */
  static replaceBeforeCursor(el, text, deleteBefore) {
    const n = Math.max(0, Math.floor(deleteBefore) || 0);
    const cm = _TextInserter.getCodeMirrorView(el);
    if (cm) {
      const head = cm.state.selection.main.head;
      const from = Math.max(0, head - n);
      cm.dispatch({
        changes: { from, to: head, insert: text },
        selection: { anchor: from + text.length }
      });
      cm.focus();
      return;
    }
    if (el.tagName && (el.tagName.toLowerCase() === "input" || el.tagName.toLowerCase() === "textarea")) {
      const inputEl = el;
      const start = inputEl.selectionStart || 0;
      const end = inputEl.selectionEnd || start;
      const from = Math.max(0, start - n);
      const tailStart = Math.max(end, start);
      const value = inputEl.value;
      inputEl.value = value.substring(0, from) + text + value.substring(tailStart);
      const newPos = from + text.length;
      inputEl.selectionStart = newPos;
      inputEl.selectionEnd = newPos;
      inputEl.focus();
      inputEl.dispatchEvent(new Event("input", { bubbles: true }));
      return;
    }
    el.focus();
    const sel = window.getSelection();
    const range = sel && sel.rangeCount > 0 ? sel.getRangeAt(0) : null;
    if (sel && range && range.startContainer.nodeType === Node.TEXT_NODE && n > 0) {
      const node = range.startContainer;
      const del = document.createRange();
      del.setStart(node, Math.max(0, range.startOffset - n));
      del.setEnd(node, range.startOffset);
      del.deleteContents();
      const textNode = document.createTextNode(text);
      del.insertNode(textNode);
      const after = document.createRange();
      after.setStartAfter(textNode);
      after.collapse(true);
      sel.removeAllRanges();
      sel.addRange(after);
      return;
    }
    if (sel && range) {
      range.deleteContents();
      range.insertNode(document.createTextNode(text));
      range.collapse(false);
      return;
    }
    for (let i = 0; i < n; i++) document.execCommand("delete");
    document.execCommand("insertText", false, text);
  }
  /**
   * 在 replaceBeforeCursor 基础上支持「插入后光标落到模板内相对位置」：
   * - cursorRel：相对 text 起点的光标偏移；null/undefined 时放文本尾（与旧行为一致）
   * - selectFrom/selectTo：相对 text 起点的选中区间（${0:默认词} 用，覆盖光标位置）
   * 仅 input/textarea 能精确到字符；contentEditable 兜底只做光标尾置。
   */
  static replaceBeforeCursorSmart(el, text, deleteBefore, cursorRel, selectFrom, selectTo) {
    const n = Math.max(0, Math.floor(deleteBefore) || 0);
    const cm = _TextInserter.getCodeMirrorView(el);
    if (cm) {
      const head = cm.state.selection.main.head;
      const from = Math.max(0, head - n);
      const len = text.length;
      const anchor = from + (selectFrom != null ? selectFrom : cursorRel != null ? cursorRel : len);
      const focus = from + (selectFrom != null && selectTo != null ? selectTo : cursorRel != null ? cursorRel : len);
      cm.dispatch({
        changes: { from, to: head, insert: text },
        selection: { anchor, head: focus }
      });
      cm.focus();
      return;
    }
    if (el.tagName && (el.tagName.toLowerCase() === "input" || el.tagName.toLowerCase() === "textarea")) {
      const inputEl = el;
      const start = inputEl.selectionStart || 0;
      const end = inputEl.selectionEnd || start;
      const from = Math.max(0, start - n);
      const tailStart = Math.max(end, start);
      const value = inputEl.value;
      inputEl.value = value.substring(0, from) + text + value.substring(tailStart);
      const len = text.length;
      const cursorPos = cursorRel != null ? Math.min(cursorRel, len) : len;
      const selFrom = from + (selectFrom != null ? Math.min(selectFrom, len) : cursorPos);
      const selTo = from + (selectTo != null ? Math.min(selectTo, len) : selFrom);
      inputEl.selectionStart = selFrom;
      inputEl.selectionEnd = selTo;
      inputEl.focus();
      inputEl.dispatchEvent(new Event("input", { bubbles: true }));
      return;
    }
    el.focus();
    const sel = window.getSelection();
    const range = sel && sel.rangeCount > 0 ? sel.getRangeAt(0) : null;
    if (sel && range && range.startContainer.nodeType === Node.TEXT_NODE && n > 0) {
      const node = range.startContainer;
      const del = document.createRange();
      del.setStart(node, Math.max(0, range.startOffset - n));
      del.setEnd(node, range.startOffset);
      del.deleteContents();
      const textNode = document.createTextNode(text);
      del.insertNode(textNode);
      const offset = Math.min(
        cursorRel != null ? cursorRel : text.length,
        text.length
      );
      const after = document.createRange();
      after.setStart(textNode, offset);
      after.collapse(true);
      sel.removeAllRanges();
      sel.addRange(after);
      return;
    }
    if (sel && range) {
      range.deleteContents();
      const textNode = document.createTextNode(text);
      range.insertNode(textNode);
      const offset = Math.min(
        cursorRel != null ? cursorRel : text.length,
        text.length
      );
      const after = document.createRange();
      after.setStart(textNode, offset);
      after.collapse(true);
      sel.removeAllRanges();
      sel.addRange(after);
      return;
    }
    for (let i = 0; i < n; i++) document.execCommand("delete");
    document.execCommand("insertText", false, text);
  }
  static replaceRange(el, text, replaceLength) {
    const cm = _TextInserter.getCodeMirrorView(el);
    if (cm) {
      const from = cm.state.selection.main.head;
      const to = from + replaceLength;
      cm.dispatch({
        changes: { from, to, insert: text },
        selection: { anchor: from + text.length }
      });
      cm.focus();
      return;
    }
    if (el.tagName && (el.tagName.toLowerCase() === "input" || el.tagName.toLowerCase() === "textarea")) {
      const inputEl = el;
      const pos = inputEl.selectionStart || 0;
      inputEl.value = inputEl.value.substring(0, pos) + text + inputEl.value.substring(pos + replaceLength);
      const newPos = pos + text.length;
      inputEl.selectionStart = newPos;
      inputEl.selectionEnd = newPos;
      inputEl.focus();
      inputEl.dispatchEvent(new Event("input", { bubbles: true }));
      return;
    }
    el.focus();
    const sel = window.getSelection();
    if (sel && sel.rangeCount > 0) {
      const range = sel.getRangeAt(0);
      range.deleteContents();
      range.insertNode(document.createTextNode(text));
      range.collapse(false);
    }
  }
};
_TextInserter._mirror = null;
var TextInserter = _TextInserter;

// src/suggestPopup.ts
var FloatingSuggestPopup = class {
  constructor(plugin) {
    this.plugin = plugin;
    this.container = null;
    this.items = [];
    this.selectedIndex = 0;
    this.onSelect = null;
    this.onClose = null;
    this.targetEl = null;
    this.triggerChar = "";
    this.prefixChar = "";
    this._cursorPos = null;
    this._keydownHandler = null;
    this._clickHandler = null;
    this._scrollHandler = null;
    this._resizeHandler = null;
    this._rafId = null;
    this._clickBindTimer = null;
    /** 上次渲染的列表签名：内容未变时复用全部 DOM 节点，跳过 innerHTML 清空重建 */
    this._renderedSig = null;
    /**
     * 用户是否已明确在弹窗中导航过（方向键或鼠标悬停）。
     * Enter 确认策略：有查询词或触发符（如 @）时，无论是否导航过都直接确认高亮项；
     * 仅「空查询常驻弹窗」（minimal-trigger 自动弹出、尚未输入文字）且未导航过时，
     * Enter 才放行给编辑器做换行。Tab 一律放行（缩进/焦点切换），不参与确认。
     */
    this.armed = false;
  }
  create() {
    if (this.container) return;
    this.container = document.body.createDiv();
    this.container.className = "blfc-suggest-popup";
    this.container.addClass("blfc-popup-hidden");
    document.body.appendChild(this.container);
  }
  show(items, targetEl, triggerChar, onSelect, onClose, cursorPos, prefixChar = "") {
    const wasVisible = this.isVisible();
    const prevItems = this.items;
    const prevSelected = this.selectedIndex;
    this.items = items;
    this.targetEl = targetEl;
    this.triggerChar = triggerChar;
    this.prefixChar = prefixChar || triggerChar;
    this.onSelect = onSelect;
    this.onClose = onClose;
    this.selectedIndex = this._retainHighlight(items, prevItems, prevSelected, wasVisible);
    this.armed = false;
    this._cursorPos = cursorPos || null;
    if (!this.container) this.create();
    if (this.isVisible()) this.unbindEvents();
    this.renderItems();
    this.container.removeClass("blfc-popup-hidden");
    this.positionNearCursor();
    if (!this.isVisible()) return;
    this.bindEvents();
    this._scheduleRefresh();
  }
  hide() {
    if (this.container) this.container.addClass("blfc-popup-hidden");
    this.unbindEvents();
    if (this.onClose) {
      const cb = this.onClose;
      this.onClose = null;
      cb();
    }
  }
  isVisible() {
    return !!this.container && !this.container.classList.contains("blfc-popup-hidden");
  }
  /** 当前弹窗的触发字符（'' 表示智能补全） */
  getTriggerChar() {
    return this.triggerChar;
  }
  /** 当前是否有 Obsidian 模态窗口打开（任何插件的 Modal 都会挂 .modal-container 到 body） */
  isModalOpen() {
    return !!document.querySelector(".modal-container");
  }
  /**
   * 按键是否来自弹窗所依附的编辑区域。
   * 弹窗的 keydown 监听挂在 document 捕获阶段，若不收窄作用域，
   * 弹窗可见期间任何输入框（命令面板、其他窗格）的 Enter / Tab 都会被吞掉。
   */
  isEventFromTarget(e) {
    if (!this.targetEl) return false;
    const target = e.target;
    if (!target) return false;
    return target === this.targetEl || this.targetEl.contains(target);
  }
  /**
   * 渲染行序列：条目前插入分组头（相邻同组不重复）。分组头不是可选条目。
   * 条目行保序等同 items —— data-index 即条目的真实下标，键盘导航只对条目生效。
   */
  buildRows() {
    const rows = [];
    let lastGroup = null;
    this.items.forEach((suggestion, index) => {
      const g = suggestion.group || "";
      if (g && g !== lastGroup) {
        rows.push({ kind: "head", text: g });
      }
      lastGroup = g || lastGroup;
      rows.push({ kind: "item", suggestion, index });
    });
    return rows;
  }
  renderRow(row) {
    if (row.kind === "head") {
      const head = this.container.createDiv();
      head.className = "blfc-suggest-head";
      head.textContent = row.text;
      this.container.appendChild(head);
      return;
    }
    const { suggestion, index } = row;
    const el = this.container.createDiv();
    el.className = "blfc-suggest-item";
    el.setAttribute("data-index", String(index));
    if (index === this.selectedIndex) {
      el.addClass("blfc-is-selected");
      el.addClass("blfc-preview-on");
    }
    const rowEl = el.createDiv();
    rowEl.className = "blfc-suggest-row";
    const dotEl = rowEl.createSpan();
    dotEl.className = `blfc-suggest-dot blfc-dot-${this.dotFamily(suggestion.type)}`;
    const nameEl = rowEl.createSpan();
    nameEl.className = "blfc-suggest-name";
    nameEl.textContent = suggestion.display || suggestion.name || "";
    rowEl.appendChild(dotEl);
    rowEl.appendChild(nameEl);
    el.appendChild(rowEl);
    const preview = suggestion.insert || suggestion.template || "";
    if (preview) {
      const previewEl = el.createDiv();
      previewEl.textContent = preview;
      previewEl.className = "blfc-suggest-preview";
      el.appendChild(previewEl);
    }
    el.addEventListener("mousedown", (e) => {
      e.preventDefault();
      e.stopPropagation();
      this.selectItem(index);
    });
    el.addEventListener("mouseenter", () => {
      this.armed = true;
      this.highlightItem(index);
    });
    this.container.appendChild(el);
  }
  renderItems() {
    if (!this.container) return;
    const rows = this.buildRows();
    const sig = rows.map(
      (r) => r.kind === "head" ? `H\0${r.text}` : `I\0${r.suggestion.display}\0${r.suggestion.insert}\0${r.suggestion.type}\0${r.suggestion.group}`
    ).join("");
    if (this._renderedSig === sig && rows.length > 0 && this.container.childElementCount === rows.length) {
      this.highlightItem(this.selectedIndex);
      return;
    }
    this._renderedSig = sig;
    this.container.replaceChildren();
    if (this.items.length === 0) {
      const empty = this.container.createDiv();
      empty.className = "blfc-suggest-empty";
      empty.textContent = "\u65E0\u5339\u914D\u7ED3\u679C";
      this.container.appendChild(empty);
      return;
    }
    rows.forEach((row) => this.renderRow(row));
  }
  /**
   * 类别 → 色点色系。
   * 只分四族：场景空间 / 角色台词 / 动作转场 / 其他。
   * 族过多会让色点本身变成噪声，反而违背降噪初衷。
   */
  dotFamily(type) {
    if (!type) return "other";
    if (type === "scene" || type === "scene_time_combo" || type === "location") return "scene";
    if (type === "character" || type === "dialogue" || type === "dialogue_combo") {
      return "character";
    }
    if (type === "action" || type === "technique" || type === "skill" || type === "transition" || type === "sound") {
      return "action";
    }
    return "other";
  }
  /**
   * 弹窗持续显示（打字筛选）时保留上次高亮项：上次选中项若仍在新的候选列表里
   * 则返回其新下标，否则返回 0。首次弹出/重新打开一律回到第 0 项。
   * 匹配按条目签名而非下标：格式条目用稳定 id，智能补全用 group|name|display|insert
   * 组合，避免列表增删/排序变化后高亮错位。
   */
  _retainHighlight(items, prevItems, prevSelected, wasVisible) {
    if (!wasVisible || prevItems.length === 0 || items.length === 0) return 0;
    const prev = prevItems[Math.min(prevSelected, prevItems.length - 1)];
    if (!prev) return 0;
    const sig = (s) => {
      var _a, _b, _c, _d;
      return s.id ? `id\0${s.id}` : `key\0${(_a = s.group) != null ? _a : ""}\0${(_b = s.name) != null ? _b : ""}\0${(_c = s.display) != null ? _c : ""}\0${(_d = s.insert) != null ? _d : ""}`;
    };
    const prevSig = sig(prev);
    const ni = items.findIndex((s) => sig(s) === prevSig);
    return ni >= 0 ? ni : 0;
  }
  highlightItem(index) {
    if (index < 0 || index >= this.items.length) return;
    this.selectedIndex = index;
    const items = this.container.querySelectorAll(".blfc-suggest-item");
    items.forEach((el, i) => {
      const item2 = el;
      const on = i === index;
      item2.classList.toggle("blfc-is-selected", on);
      item2.classList.toggle("blfc-preview-on", on);
    });
    const item = items[index];
    if (item && this.container) {
      const listTop = this.container.getBoundingClientRect().top;
      const listBottom = this.container.getBoundingClientRect().bottom;
      const itemTop = item.getBoundingClientRect().top;
      const itemBottom = item.getBoundingClientRect().bottom;
      if (itemTop < listTop) this.container.scrollTop += itemTop - listTop;
      else if (itemBottom > listBottom) this.container.scrollTop += itemBottom - listBottom;
    }
  }
  selectItem(index) {
    if (index < 0 || index >= this.items.length) return;
    const suggestion = this.items[index];
    const prefixChar = this.prefixChar;
    this.hide();
    if (this.onSelect) {
      const cb = this.onSelect;
      this.onSelect = null;
      cb(suggestion, prefixChar);
    }
  }
  positionNearCursor() {
    if (!this.container) return;
    const pos = this._measureCursorPos();
    if (pos) this._applyPosition(pos);
  }
  /** 实时测量光标位置（每次都以当前光标为准）；失败时回退到打开时传入的快照 */
  _measureCursorPos() {
    if (this.targetEl) {
      const cm = TextInserter.getCodeMirrorView(this.targetEl);
      if (cm) {
        try {
          const head = cm.state.selection.main.head;
          const coords = cm.coordsAtPos(head);
          if (coords && Number.isFinite(coords.left) && Number.isFinite(coords.bottom)) {
            return {
              x: coords.left,
              y: coords.bottom,
              height: coords.bottom - coords.top
            };
          }
        } catch (e) {
        }
        return this._cursorPos || null;
      }
      const el = this.targetEl;
      const isPlainEditable = el.tagName === "TEXTAREA" || el.tagName === "INPUT" || el.isContentEditable;
      if (isPlainEditable) {
        try {
          const p = TextInserter.getCursorScreenPosition(el);
          if (p && Number.isFinite(p.x) && Number.isFinite(p.y)) return p;
        } catch (e) {
        }
      }
    }
    return this._cursorPos || null;
  }
  /** 按光标坐标排版，并做视口避让：贴光标、不溢出、滚动时跟随 */
  _applyPosition(pos) {
    const container = this.container;
    const vw = window.innerWidth;
    const vh = window.innerHeight;
    const MARGIN = 8;
    const GAP = 4;
    if (pos.y < -MARGIN || pos.y > vh + MARGIN) {
      this.hide();
      return;
    }
    const popupWidth = container.offsetWidth || 200;
    const popupHeight = container.offsetHeight || Math.min(this.items.length * 28 + 6, 200);
    let left = pos.x;
    let maxWidth = "none";
    const maxLeft = vw - MARGIN - popupWidth;
    if (maxLeft < MARGIN) {
      maxWidth = `${vw - MARGIN * 2}px`;
      left = MARGIN;
    } else {
      if (left > maxLeft) left = maxLeft;
      if (left < MARGIN) left = MARGIN;
    }
    let top = pos.y + GAP;
    let maxHeight = "200px";
    const belowSpace = vh - top - MARGIN;
    if (popupHeight > belowSpace) {
      if (belowSpace >= 48) {
        maxHeight = `${Math.floor(belowSpace)}px`;
      } else {
        maxHeight = "200px";
        top = pos.y - popupHeight - GAP;
      }
    }
    if (top < MARGIN) top = MARGIN;
    setCssVar(container, "--blfc-pop-left", `${left}px`);
    setCssVar(container, "--blfc-pop-top", `${top}px`);
    setCssVar(container, "--blfc-pop-maxw", maxWidth);
    setCssVar(container, "--blfc-pop-maxh", maxHeight);
  }
  /** 用 rAF 合并高频事件（滚动/缩放），弹窗显示时始终贴近光标 */
  _scheduleRefresh() {
    if (!this.isVisible()) return;
    if (this._rafId != null) return;
    this._rafId = window.requestAnimationFrame(() => {
      this._rafId = null;
      if (this.isVisible()) this.positionNearCursor();
    });
  }
  bindEvents() {
    this._keydownHandler = (e) => {
      if (!this.isVisible()) return;
      if (this.isModalOpen()) {
        this.hide();
        return;
      }
      if (e.isComposing || e.keyCode === 229) return;
      switch (e.key) {
        case "ArrowDown":
          if (!this.isEventFromTarget(e)) return;
          e.preventDefault();
          e.stopPropagation();
          this.armed = true;
          this.highlightItem(Math.min(this.selectedIndex + 1, this.items.length - 1));
          break;
        case "ArrowUp":
          if (!this.isEventFromTarget(e)) return;
          e.preventDefault();
          e.stopPropagation();
          this.armed = true;
          this.highlightItem(Math.max(this.selectedIndex - 1, 0));
          break;
        case "Enter":
          if (!this.isEventFromTarget(e)) return;
          if (!this.armed && this.prefixChar.length === 0 && this.triggerChar === "") {
            this.hide();
            return;
          }
          e.preventDefault();
          e.stopPropagation();
          this.selectItem(this.selectedIndex);
          break;
        case "Escape":
          e.preventDefault();
          e.stopPropagation();
          this.hide();
          break;
      }
    };
    this._clickHandler = (e) => {
      if (this.isVisible() && this.container && !this.container.contains(e.target)) {
        this.hide();
      }
    };
    this._scrollHandler = () => this._scheduleRefresh();
    this._resizeHandler = () => this._scheduleRefresh();
    document.addEventListener("keydown", this._keydownHandler, true);
    document.addEventListener("scroll", this._scrollHandler, true);
    window.addEventListener("resize", this._resizeHandler);
    if (this._clickBindTimer != null) {
      window.clearTimeout(this._clickBindTimer);
      this._clickBindTimer = null;
    }
    this._clickBindTimer = window.setTimeout(() => {
      this._clickBindTimer = null;
      document.addEventListener("mousedown", this._clickHandler, true);
    }, 50);
  }
  unbindEvents() {
    if (this._keydownHandler) {
      document.removeEventListener("keydown", this._keydownHandler, true);
      this._keydownHandler = null;
    }
    if (this._clickHandler) {
      document.removeEventListener("mousedown", this._clickHandler, true);
      this._clickHandler = null;
    }
    if (this._scrollHandler) {
      document.removeEventListener("scroll", this._scrollHandler, true);
      this._scrollHandler = null;
    }
    if (this._resizeHandler) {
      window.removeEventListener("resize", this._resizeHandler);
      this._resizeHandler = null;
    }
    if (this._rafId != null) {
      window.cancelAnimationFrame(this._rafId);
      this._rafId = null;
    }
    if (this._clickBindTimer != null) {
      window.clearTimeout(this._clickBindTimer);
      this._clickBindTimer = null;
    }
  }
  destroy() {
    this.hide();
    if (this.container && this.container.parentNode) {
      this.container.remove();
      this.container = null;
    }
  }
};

// src/formatEngine.ts
var pad = (n) => String(n).padStart(2, "0");
function offsetToLineCh(text, offset) {
  var _a, _b;
  const safe = Math.max(0, Math.min(offset, text.length));
  const parts = text.split("\n");
  let rem = safe;
  for (let i = 0; i < parts.length; i++) {
    const len = parts[i].length;
    if (rem <= len) return { line: i, ch: rem };
    rem -= len + 1;
  }
  const last = parts.length - 1;
  return { line: last, ch: (_b = (_a = parts[last]) == null ? void 0 : _a.length) != null ? _b : 0 };
}
function ensureTrailingSpace(text) {
  if (!text) return text;
  return /\s$/.test(text) ? text : text + " ";
}
function renderFormatTemplate(template, ctx) {
  const out = [];
  let cursorOffset = null;
  let selectFrom = null;
  let selectTo = null;
  let sceneNoCache = null;
  const sceneNo = () => {
    if (sceneNoCache === null) {
      sceneNoCache = ctx.getSceneNumber ? ctx.getSceneNumber() : "1.1";
    }
    return sceneNoCache;
  };
  const now = ctx.now || /* @__PURE__ */ new Date();
  const stamp = {
    date: `${now.getFullYear()}-${pad(now.getMonth() + 1)}-${pad(now.getDate())}`,
    time: `${pad(now.getHours())}:${pad(now.getMinutes())}`,
    datetime: `${now.getFullYear()}-${pad(now.getMonth() + 1)}-${pad(now.getDate())}-${pad(
      now.getHours()
    )}:${pad(now.getMinutes())}`
  };
  const valueOf = (name) => {
    var _a;
    switch (name) {
      case "episode": {
        const [e] = sceneNo().split(".");
        return { found: true, value: e || "1" };
      }
      case "scene": {
        const [, s] = sceneNo().split(".");
        return { found: true, value: s || "1" };
      }
      case "scenetype":
        return { found: true, value: ctx.sceneType === "ext" ? "\u5916\u666F" : "\u5185\u666F" };
      case "date":
        return { found: true, value: stamp.date };
      case "time":
        return { found: true, value: stamp.time };
      case "datetime":
        return { found: true, value: stamp.datetime };
      case "selection":
        return { found: true, value: (_a = ctx.selection) != null ? _a : "" };
      default:
        return { found: false, value: "" };
    }
  };
  let hasCursorMarker = false;
  let i = 0;
  const t = template || "";
  while (i < t.length) {
    const c = t[i];
    if (c === "\\" && i + 1 < t.length && "{}.$\\".includes(t[i + 1])) {
      out.push(t[i + 1]);
      i += 2;
      continue;
    }
    if (c === "$") {
      if (t.startsWith("${0:", i)) {
        const close = t.indexOf("}", i + 4);
        if (close >= 0) {
          const def = t.slice(i + 4, close);
          const at = out.join("").length;
          if (!hasCursorMarker) {
            hasCursorMarker = true;
            cursorOffset = at + def.length;
            selectFrom = at;
            selectTo = at + def.length;
          }
          out.push(def);
          i = close + 1;
          continue;
        }
      }
      if (t[i + 1] === "0") {
        if (!hasCursorMarker) {
          hasCursorMarker = true;
          cursorOffset = out.join("").length;
        }
        i += 2;
        continue;
      }
      out.push(c);
      i += 1;
      continue;
    }
    if (c === "{") {
      const close = t.indexOf("}", i + 1);
      if (close >= 0) {
        const name = t.slice(i + 1, close);
        const v = valueOf(name);
        if (v.found) {
          out.push(v.value);
          i = close + 1;
          continue;
        }
        if (cursorOffset === null) {
          console.warn(`[InFlow] \u672A\u77E5\u6A21\u677F\u53D8\u91CF {${name}}\uFF0C\u5DF2\u6309\u539F\u6837\u8F93\u51FA`);
        }
        out.push(t.slice(i, close + 1));
        i = close + 1;
        continue;
      }
    }
    out.push(c);
    i += 1;
  }
  let text = out.join("");
  if (!hasCursorMarker) {
    text = ensureTrailingSpace(text);
  }
  return {
    text,
    cursorOffset,
    selectFrom,
    selectTo
  };
}

// src/inputListener.ts
var GlobalInputListener = class {
  constructor(plugin) {
    this.plugin = plugin;
    this._inputHandler = null;
    this._editorChangeRef = null;
    this.lastTriggerTime = 0;
    this.isActive = false;
    this.popup = new FloatingSuggestPopup(plugin);
  }
  activate() {
    if (this.isActive) return;
    this.isActive = true;
    this.popup.create();
    this._inputHandler = (e) => this.onInput(e);
    document.addEventListener("input", this._inputHandler, true);
    this._editorChangeRef = this.plugin.app.workspace.on(
      "editor-change",
      (editor) => this.onEditorChange(editor)
    );
    if (this._editorChangeRef) this.plugin.registerEvent(this._editorChangeRef);
  }
  deactivate() {
    this.isActive = false;
    if (this._inputHandler) {
      document.removeEventListener("input", this._inputHandler, true);
      this._inputHandler = null;
    }
    this.popup.destroy();
  }
  // ---- 非 CodeMirror 输入（input / textarea / contentEditable） ----
  onInput(_e) {
    if (!this.plugin.settings.enabled) return;
    const el = document.activeElement;
    if (!el || !TextInserter.isEditable(el)) return;
    if (TextInserter.isInExcludedContainer(el)) return;
    if (TextInserter.getCodeMirrorView(el)) return;
    this._processText(TextInserter.getTextBeforeCursor(el), el, null);
  }
  // ---- Obsidian CodeMirror 编辑器 ----
  onEditorChange(editor) {
    if (!this.plugin.settings.enabled) return;
    if (!editor) return;
    const cursor = editor.getCursor();
    if (!cursor) return;
    const line = editor.getLine(cursor.line);
    const textBefore = line.substring(0, cursor.ch);
    const cmDom = editor.containerEl || null;
    this._processText(textBefore, cmDom || document.activeElement, editor);
  }
  // ---- 统一处理逻辑 ----
  _processText(textBefore, el, cmEditor) {
    if (!textBefore) {
      if (this.popup.isVisible()) this.popup.hide();
      return;
    }
    const cursorPos = cmEditor ? this._getCMCursorPos(cmEditor, el) : null;
    const tmatch = this.plugin.formatsManager.findTriggerMatch(textBefore);
    if (tmatch) {
      const suggestions = this.plugin.formatsManager.expandGroups(tmatch.groupIds);
      if (suggestions.length > 0) {
        this.popup.show(
          suggestions,
          el,
          tmatch.char,
          (s, prefixChar) => cmEditor ? this._onFormatSelectCM(cmEditor, s, prefixChar) : this._onFormatSelectDOM(el, s, prefixChar),
          () => {
          },
          cursorPos,
          tmatch.char
        );
        return;
      }
    }
    if (this.popup.isVisible() && this.popup.getTriggerChar() !== "" && !textBefore.endsWith(this.popup.getTriggerChar())) {
      this.popup.hide();
    }
    if (!this.plugin.settings.enableSmartCompletion) return;
    const now = Date.now();
    if (now - this.lastTriggerTime < 150) {
      if (this.popup.isVisible()) this.popup.positionNearCursor();
      return;
    }
    this.lastTriggerTime = now;
    const lineText = cmEditor ? cmEditor.getLine(cmEditor.getCursor().line) : TextInserter.getCurrentLineText(el);
    const contextType = this.detectLineContext(lineText);
    const resolved = this.plugin.resolveCompletion(textBefore, contextType);
    let toShow = resolved;
    if (!toShow && this.plugin.settings.enableMinimalTrigger && (contextType === "scene_location" || contextType === "character_name" || contextType === "action")) {
      const minimal = this.plugin.getSmartSuggestionsForContext(contextType, "");
      if (minimal.length > 0) toShow = { query: "", suggestions: minimal };
    }
    if (toShow) {
      this.popup.show(
        toShow.suggestions,
        el,
        "",
        (s, prefixChar) => cmEditor ? this._onSmartSelectCM(cmEditor, s, prefixChar) : this._onSmartSelectDOM(el, s, prefixChar),
        () => {
        },
        cursorPos,
        toShow.query
      );
    } else if (this.popup.isVisible()) {
      this.popup.hide();
    }
  }
  _getCMCursorPos(editor, fallbackEl) {
    try {
      const containerEl = editor.containerEl;
      const view = containerEl ? TextInserter.getCodeMirrorView(containerEl) : null;
      if (view) {
        const head = view.state.selection.main.head;
        const coords = view.coordsAtPos(head);
        if (coords) {
          return {
            x: coords.left,
            y: coords.bottom,
            height: coords.bottom - coords.top
          };
        }
      }
    } catch (e) {
    }
    const scope = fallbackEl || editor.containerEl;
    let contentEl = null;
    if (scope) {
      const cmEl = scope.querySelector(".cm-editor") || (scope.classList.contains("cm-editor") ? scope : null);
      contentEl = (cmEl ? cmEl.querySelector(".cm-content") : null) || (scope.classList.contains("cm-content") ? scope : null) || cmEl || scope;
    }
    if (contentEl) {
      const rect = contentEl.getBoundingClientRect();
      return { x: rect.left + 40, y: rect.top + 40, height: 20 };
    }
    return null;
  }
  /** 计算选中项需要覆盖（替换）的字符数，详见 TextInserter.calcReplaceLength */
  _calcDeleteLength(textBefore, prefixChar) {
    return TextInserter.calcReplaceLength(textBefore, prefixChar);
  }
  // ---- 选择回调：两条路径统一交给 main.insertFormatItem 渲染插入 ----
  _onFormatSelectDOM(el, suggestion, prefixChar) {
    const len = this._calcDeleteLength(TextInserter.getTextBeforeCursor(el), prefixChar);
    this.plugin.insertFormatItem(null, el, suggestion, len);
  }
  /**
   * 确认前的过期守卫：弹窗打开到 Enter 确认之间若发生 IME 上屏 / 其它并发改动，
   * 光标前的文本可能已不再包含本次确认的查询词（prefixChar 非空但算不出替换长度）。
   * 此时继续插入会把补全文本落到错误坐标 —— 丢弃本次插入比强插更安全。
   */
  _isStaleConfirm(textBefore, prefixChar) {
    return !!prefixChar && TextInserter.calcReplaceLength(textBefore, prefixChar) === 0;
  }
  _onSmartSelectDOM(el, suggestion, prefixChar) {
    var _a;
    const textBefore = TextInserter.getTextBeforeCursor(el);
    const len = this._calcDeleteLength(textBefore, prefixChar);
    if (this._isStaleConfirm(textBefore, prefixChar)) {
      console.warn(
        `[InFlow] \u667A\u80FD\u8865\u5168\u5DF2\u8FC7\u671F\uFF08\u5149\u6807\u524D\u672A\u627E\u5230\u67E5\u8BE2\u8BCD\u300C${prefixChar}\u300D\uFF09\uFF0C\u5DF2\u53D6\u6D88\u63D2\u5165`
      );
      return;
    }
    const insert = (_a = suggestion.insert) != null ? _a : "";
    if (!insert) return;
    TextInserter.replaceBeforeCursor(el, insert, len);
    this.plugin.recordUsage(suggestion);
  }
  // ---- 选择回调：CM 编辑器的 Obsidian Editor API ----
  _onFormatSelectCM(editor, suggestion, prefixChar) {
    const cursor = editor.getCursor();
    const textBefore = editor.getLine(cursor.line).substring(0, cursor.ch);
    const len = this._calcDeleteLength(textBefore, prefixChar);
    this.plugin.insertFormatItem(editor, null, suggestion, len);
  }
  _onSmartSelectCM(editor, suggestion, prefixChar) {
    var _a;
    const cursor = editor.getCursor();
    const textBefore = editor.getLine(cursor.line).substring(0, cursor.ch);
    const len = this._calcDeleteLength(textBefore, prefixChar);
    if (this._isStaleConfirm(textBefore, prefixChar)) {
      console.warn(
        `[InFlow] \u667A\u80FD\u8865\u5168\u5DF2\u8FC7\u671F\uFF08\u5149\u6807\u524D\u672A\u627E\u5230\u67E5\u8BE2\u8BCD\u300C${prefixChar}\u300D\uFF09\uFF0C\u5DF2\u53D6\u6D88\u63D2\u5165`
      );
      return;
    }
    const insert = (_a = suggestion.insert) != null ? _a : "";
    if (!insert) return;
    const base = { line: cursor.line, ch: Math.max(0, cursor.ch - len) };
    editor.replaceRange(insert, base, cursor);
    const rel = offsetToLineCh(insert, insert.length);
    editor.setCursor({
      line: base.line + rel.line,
      ch: rel.line === 0 ? base.ch + rel.ch : rel.ch
    });
    this.plugin.recordUsage(suggestion);
  }
  detectLineContext(line) {
    if (!line) return "general";
    if (/^##\s/.test(line)) return "scene_location";
    if (/^####\s/.test(line)) return "character_name";
    if (/^#####\s/.test(line)) return "character_name";
    if (/^`\[.*\]`$/.test(line)) return "action";
    if (/^\*\(.*\)\*$/.test(line)) return "action";
    return "general";
  }
};

// src/sceneNumberGenerator.ts
var EPISODE_TITLE_RE = /^#+\s*第\s*([零一二三四五六七八九十百千万\d]+|[0-9]+)\s*集/;
var CHINESE_NUMBERS = {
  \u96F6: 0,
  "\u3007": 0,
  \u4E00: 1,
  \u4E8C: 2,
  \u4E09: 3,
  \u56DB: 4,
  \u4E94: 5,
  \u516D: 6,
  \u4E03: 7,
  \u516B: 8,
  \u4E5D: 9,
  \u5341: 10,
  \u767E: 100,
  \u5343: 1e3,
  \u4E07: 1e4
};
var SCENE_LINE_RE = /^(##\s*)(\d+\.\d+)(\s*[^\s].*)$/;
var SceneNumberGenerator = class {
  /** 获取下一个场景编号（形如 1.1 / 1.2） */
  getNextSceneNumber(editor, cursor) {
    const episode = this.detectCurrentEpisode(editor, cursor);
    const maxSceneNumber = this.getMaxSceneNumberInEpisode(editor, episode);
    return `${episode}.${maxSceneNumber + 1}`;
  }
  /** 从光标位置向上回溯，识别当前所属集数 */
  detectCurrentEpisode(editor, cursor) {
    let currentLine = cursor.line;
    while (currentLine >= 0) {
      const lineContent = editor.getLine(currentLine).trim();
      const episodeMatch = lineContent.match(EPISODE_TITLE_RE);
      if (episodeMatch) {
        return this.chineseToNumber(episodeMatch[1]);
      }
      currentLine--;
    }
    return 1;
  }
  /** 中文数字转阿拉伯数字（含混合型如"一百二十三"） */
  chineseToNumber(chinese) {
    if (/^\d+$/.test(chinese)) return parseInt(chinese, 10);
    let total = 0;
    let temp = 0;
    for (let i = 0; i < chinese.length; i++) {
      const char = chinese[i];
      const value = CHINESE_NUMBERS[char];
      if (value === void 0) {
        const num = parseInt(chinese, 10);
        return isNaN(num) ? 1 : num;
      }
      if (value < 10) {
        temp = value;
      } else if (value === 10) {
        total += (temp === 0 ? 1 : temp) * value;
        temp = 0;
      } else {
        total += (temp === 0 ? 1 : temp) * value;
        temp = 0;
      }
    }
    total += temp;
    return total === 0 ? 1 : total;
  }
  /** 获取当前集内已存在的最大场景号 */
  getMaxSceneNumberInEpisode(editor, episode) {
    let maxSceneNumber = 0;
    const lineCount = editor.lineCount();
    const range = this.findEpisodeRange(editor, episode);
    if (!range) return 0;
    for (let i = range.start; i <= range.end; i++) {
      const lineContent = editor.getLine(i);
      const sceneMatch = lineContent.match(/^##\s*(\d+)\.(\d+)\s*[^\s]/);
      if (sceneMatch) {
        const sceneEp = parseInt(sceneMatch[1], 10);
        const sceneNum = parseInt(sceneMatch[2], 10);
        if (sceneEp === episode && sceneNum > maxSceneNumber) {
          maxSceneNumber = sceneNum;
        }
      }
    }
    return maxSceneNumber;
  }
  /** 重编号当前光标所在集 */
  renumberCurrentEpisode(editor) {
    const cursor = editor.getCursor();
    const episode = this.detectCurrentEpisode(editor, cursor);
    const episodeRange = this.findEpisodeRange(editor, episode);
    if (!episodeRange) return { success: false, renumberedScenes: 0 };
    return this.renumberScenesInRange(editor, episode, episodeRange.start, episodeRange.end);
  }
  /** 重编号全部集的全部场景 */
  renumberAllEpisodes(editor) {
    const episodes = this.findAllEpisodes(editor);
    let totalRenumbered = 0;
    let episodesRenumbered = 0;
    for (const episodeInfo of episodes) {
      const result = this.renumberScenesInRange(
        editor,
        episodeInfo.episode,
        episodeInfo.startLine,
        episodeInfo.endLine
      );
      if (result.success) {
        totalRenumbered += result.renumberedScenes;
        episodesRenumbered++;
      }
    }
    return {
      success: totalRenumbered > 0,
      renumberedScenes: totalRenumbered,
      totalRenumbered,
      episodesRenumbered
    };
  }
  /**
   * 单次遍历求所有集的标题行与行区间（O(n)）。
   * 旧实现为每集各做一次全文档扫描，重编号全部集时为 O(n·E)。
   */
  findEpisodeBoundaries(editor) {
    const lineCount = editor.lineCount();
    const titles = [];
    for (let i = 0; i < lineCount; i++) {
      const m = editor.getLine(i).trim().match(EPISODE_TITLE_RE);
      if (m) titles.push({ episode: this.chineseToNumber(m[1]), line: i });
    }
    return titles.map((t, idx) => {
      const endLine = idx + 1 < titles.length ? titles[idx + 1].line - 1 : lineCount - 1;
      return { episode: t.episode, titleLine: t.line, startLine: t.line, endLine };
    });
  }
  /** 查找指定集数在文档中的行区间 */
  findEpisodeRange(editor, episode) {
    const b = this.findEpisodeBoundaries(editor).find((x) => x.episode === episode);
    return b ? { start: b.startLine, end: b.endLine } : null;
  }
  /** 在指定行区间内按顺序重编号场景 */
  renumberScenesInRange(editor, episode, startLine, endLine) {
    let sceneCounter = 1;
    let renumberedScenes = 0;
    for (let i = startLine; i <= endLine; i++) {
      const lineContent = editor.getLine(i);
      const sceneMatch = lineContent.match(SCENE_LINE_RE);
      if (sceneMatch) {
        const prefix = sceneMatch[1];
        const oldNumber = sceneMatch[2];
        const suffix = sceneMatch[3];
        const newNumber = `${episode}.${sceneCounter}`;
        if (oldNumber !== newNumber && editor.setLine) {
          editor.setLine(i, `${prefix}${newNumber}${suffix}`);
          renumberedScenes++;
        }
        sceneCounter++;
      }
    }
    return {
      success: renumberedScenes > 0,
      renumberedScenes
    };
  }
  /** 列出文档中所有集及其区间（基于单次遍历结果，O(n)） */
  findAllEpisodes(editor) {
    return this.findEpisodeBoundaries(editor).map((b) => ({
      episode: b.episode,
      startLine: b.startLine,
      endLine: b.endLine
    }));
  }
};

// src/settingsTab.ts
var import_obsidian6 = require("obsidian");

// src/formatModals.ts
var import_obsidian4 = require("obsidian");
function getActiveEditor(app) {
  const active = app.workspace.activeEditor;
  return active && active.editor ? active.editor : null;
}
function describeRender(r) {
  if (!r.text) return "\uFF08\u7A7A\u6587\u672C\uFF09";
  let s = JSON.stringify(r.text).replace(/^"|"$/g, "").replace(/\\n/g, "\u21B5\n");
  if (r.selectFrom != null && r.selectTo != null) {
    s += `
\u23FA \u9ED8\u8BA4\u8BCD\u300C${r.text.slice(r.selectFrom, r.selectTo)}\u300D\u63D2\u5165\u540E\u5C06\u88AB\u9009\u4E2D`;
  } else if (r.cursorOffset != null) {
    s += `
\u23FA $0 \u5149\u6807\u843D\u70B9\uFF1A\u504F\u79FB ${r.cursorOffset}`;
  }
  return s;
}
var FormatSuggestModal = class extends import_obsidian4.FuzzySuggestModal {
  constructor(app, plugin, groupIds) {
    super(app);
    this.plugin = plugin;
    this.groupIds = groupIds;
    this.setPlaceholder("\u9009\u62E9\u8981\u63D2\u5165\u7684\u6A21\u677F\u2026");
  }
  getItems() {
    return this.plugin.formatsManager.expandGroups(this.groupIds);
  }
  getItemText(item) {
    return item.display || item.name || "";
  }
  onChooseItem(item) {
    const editor = getActiveEditor(this.app);
    if (!editor) {
      new import_obsidian4.Notice("\u6CA1\u6709\u6D3B\u52A8\u7684\u7F16\u8F91\u5668");
      return;
    }
    this.plugin.insertFormatItem(editor, null, item, 0);
  }
};
var FormatItemEditModal = class extends import_obsidian4.Modal {
  constructor(app, plugin, item, onSave) {
    super(app);
    this.plugin = plugin;
    this.item = item;
    this.onSave = onSave;
  }
  onOpen() {
    const { contentEl } = this;
    contentEl.empty();
    contentEl.addClass("blfc-plugin");
    const isNew = this.item.id.startsWith("custom-");
    contentEl.createEl("h3", { text: isNew ? "\u6DFB\u52A0\u6A21\u677F" : "\u7F16\u8F91\u6A21\u677F" });
    new import_obsidian4.Setting(contentEl).setName("\u540D\u79F0").setDesc("\u83DC\u5355\u4E2D\u663E\u793A\u7684\u6761\u76EE\u540D").addText((t) => {
      t.setValue(this.item.name).onChange((v) => {
        this.item.name = v;
      });
    });
    const groups = this.plugin.formatsManager.file.groups;
    new import_obsidian4.Setting(contentEl).setName("\u6240\u5C5E\u5206\u7EC4").setDesc("\u8BE5\u6761\u76EE\u51FA\u73B0\u5728\u54EA\u4E2A\u5206\u7EC4\u4E0B\uFF08\u53EF\u7A0D\u540E\u8C03\u6574\uFF09").addDropdown((d) => {
      var _a;
      groups.forEach((g) => {
        d.addOption(g.id, g.name);
      });
      const cur = groups.some((g) => g.id === this.item.group) ? this.item.group : ((_a = groups[0]) == null ? void 0 : _a.id) || "";
      d.setValue(cur).onChange((v) => {
        this.item.group = v;
      });
      if (cur) this.item.group = cur;
    });
    new import_obsidian4.Setting(contentEl).setName("\u6587\u672C").setDesc(
      "\u652F\u6301\u53D8\u91CF {date} {time} {datetime} {selection}\uFF08\u5267\u672C\u6761\u76EE\u53E6\u6709 {episode} {scene} {scenetype}\uFF09\uFF1B$0 \u63D2\u5165\u540E\u5149\u6807\u843D\u70B9\uFF1B${0:\u9ED8\u8BA4\u8BCD} \u843D\u70B9\u5E76\u9009\u4E2D\uFF1B\\{ \\$ \\\\ \u8F6C\u4E49\u5B57\u9762\u91CF\u3002"
    ).addTextArea((ta) => {
      ta.setValue(this.item.text).onChange((v) => {
        this.item.text = v;
      }).inputEl.rows = 3;
    });
    const previewWrap = contentEl.createDiv({ cls: "blfc-fmt-edit-preview" });
    previewWrap.createDiv({
      text: "\u63D2\u5165\u6548\u679C\u9884\u89C8\uFF08\u573A\u666F\u7F16\u53F7\u4EE5 1.1 \u793A\u8303\uFF09\uFF1A",
      cls: "blfc-fmt-edit-preview-label"
    });
    const previewBody = previewWrap.createEl("pre", {
      cls: "blfc-fmt-edit-preview-body"
    });
    const refreshPreview = () => {
      const r = renderFormatTemplate(this.item.text, {
        sceneType: "int",
        getSceneNumber: () => "1.1"
      });
      previewBody.textContent = describeRender(r);
    };
    refreshPreview();
    const textarea = contentEl.querySelector("textarea");
    if (textarea) textarea.addEventListener("input", refreshPreview);
    new import_obsidian4.Setting(contentEl).addButton(
      (btn) => btn.setButtonText("\u4FDD\u5B58").setCta().onClick(() => {
        if (!this.item.name.trim()) {
          new import_obsidian4.Notice("\u540D\u79F0\u4E0D\u80FD\u4E3A\u7A7A");
          return;
        }
        if (!this.item.text.trim()) {
          new import_obsidian4.Notice("\u6587\u672C\u4E0D\u80FD\u4E3A\u7A7A");
          return;
        }
        this.onSave({
          ...this.item,
          name: this.item.name.trim(),
          text: this.item.text,
          group: this.item.group
        });
        this.close();
      })
    ).addButton((btn) => btn.setButtonText("\u53D6\u6D88").onClick(() => this.close()));
  }
  onClose() {
    this.contentEl.empty();
  }
};
var FormatsTransferModal = class extends import_obsidian4.Modal {
  constructor(app, plugin, mode) {
    super(app);
    this.plugin = plugin;
    this.mode = mode;
  }
  onOpen() {
    const { contentEl } = this;
    contentEl.empty();
    contentEl.addClass("blfc-plugin");
    const isExport = this.mode === "export";
    contentEl.createEl("h3", { text: isExport ? "\u5BFC\u51FA\u6A21\u677F\u914D\u7F6E" : "\u5BFC\u5165\u6A21\u677F\u914D\u7F6E" });
    contentEl.createEl("p", {
      text: isExport ? "\u590D\u5236\u4EE5\u4E0B JSON \u4FDD\u5B58\u6216\u5206\u4EAB\u3002" : "\u7C98\u8D34 formats.json \u5185\u5BB9\u540E\u70B9\u51FB\u5BFC\u5165\uFF08\u65E7\u7248\u6587\u4EF6\u4F1A\u81EA\u52A8\u8FC1\u79FB\u4E3A\u65E0\u5185\u7F6E v4 \u7ED3\u6784\uFF09\u3002",
      cls: "blfc-fmt-transfer-desc"
    });
    const ta = contentEl.createEl("textarea", {
      cls: "blfc-fmt-transfer-textarea"
    });
    if (isExport) {
      ta.value = this.plugin.formatsManager.exportJson();
      ta.readOnly = true;
    }
    ta.rows = 18;
    ta.addEventListener("keydown", (e) => e.stopPropagation());
    new import_obsidian4.Setting(contentEl).addButton((btn) => {
      if (isExport) {
        btn.setButtonText("\u590D\u5236\u5168\u90E8").onClick(() => {
          ta.select();
          document.execCommand("copy");
          new import_obsidian4.Notice("\u5DF2\u590D\u5236\u5230\u526A\u8D34\u677F");
        });
      } else {
        btn.setButtonText("\u5BFC\u5165").setCta().onClick(() => {
          const res = this.plugin.formatsManager.importJson(ta.value);
          new import_obsidian4.Notice(res.message);
          if (res.ok) this.close();
        });
      }
      return btn;
    }).addButton((btn) => btn.setButtonText("\u5173\u95ED").onClick(() => this.close()));
  }
  onClose() {
    this.contentEl.empty();
  }
};

// src/formatsManager.ts
var import_obsidian5 = require("obsidian");
var FORMATS_DIR = ".inflow";
var FORMATS_FILE_PATH = `${FORMATS_DIR}/formats.json`;
var MAX_QUICK_COMMANDS = 4;
var FORMATS_VERSION = 4;
var DEFAULT_TRIGGER_CHAR = "@";
var clone = (x) => JSON.parse(JSON.stringify(x));
var FormatsManager = class {
  constructor(plugin) {
    this.plugin = plugin;
    this._data = this.emptyFormats();
  }
  get file() {
    return this._data;
  }
  /** 全新空配置（无任何预置；分组 / 条目全部由用户自建） */
  emptyFormats() {
    return {
      version: FORMATS_VERSION,
      triggerChar: DEFAULT_TRIGGER_CHAR,
      groups: [],
      items: [],
      quickCommands: []
    };
  }
  async initialize() {
    const adapter = this.plugin.app.vault.adapter;
    try {
      if (!await adapter.exists(FORMATS_DIR)) {
        await adapter.mkdir(FORMATS_DIR).catch(() => {
        });
      }
      if (await adapter.exists(FORMATS_FILE_PATH)) {
        try {
          const raw = await adapter.read(FORMATS_FILE_PATH);
          const { file, changed } = this.parse(raw);
          this._data = file;
          if (changed) {
            await this.save();
          }
          return;
        } catch (e) {
          console.error("[InFlow] formats.json \u89E3\u6790\u5931\u8D25\uFF0C\u91CD\u5EFA\u7A7A\u914D\u7F6E:", e);
        }
      }
      this._data = this.emptyFormats();
      await this.save();
    } catch (e) {
      console.error("[InFlow] \u521D\u59CB\u5316 formats.json \u5931\u8D25\uFF0C\u4F7F\u7528\u5185\u5B58\u7A7A\u914D\u7F6E:", e);
      this._data = this.emptyFormats();
    }
  }
  /**
   * 解析 + 迁移 → v4。
   * 返回 changed = 是否需要落盘（发生过迁移 / 清理孤儿 / 剥除内置残留）。
   */
  parse(raw) {
    const obj = JSON.parse(raw);
    if (!obj || typeof obj !== "object") throw new Error("\u5185\u5BB9\u4E3A\u7A7A");
    const isCurrentLike = typeof obj.triggerChar === "string" && Array.isArray(obj.groups) && Array.isArray(obj.items);
    let file;
    let changed = false;
    if (isCurrentLike) {
      const g = obj;
      file = {
        version: FORMATS_VERSION,
        triggerChar: g.triggerChar,
        groups: Array.isArray(g.groups) ? g.groups : [],
        items: Array.isArray(g.items) ? g.items : [],
        quickCommands: Array.isArray(g.quickCommands) ? g.quickCommands : []
      };
      changed = obj.version !== FORMATS_VERSION || this.builtinCount(file) > 0 || obj.builtinSnapshots !== void 0;
      file = this.stripBuiltins(file);
      file = this.homeOrphans(file);
    } else {
      file = this.migrateLegacy(obj);
      changed = true;
    }
    const validGroups = new Set(file.groups.map((grp) => grp.id));
    const before = file.items.length;
    file.items = file.items.filter((i) => validGroups.has(i.group));
    if (file.items.length !== before) changed = true;
    return { file, changed };
  }
  /** 统计带 builtin 标记的实体数（旧版残留判断用） */
  builtinCount(f) {
    let n = 0;
    f.groups.forEach((g) => {
      if (g.builtin) n++;
    });
    f.items.forEach((i) => {
      if (i.builtin) n++;
    });
    return n;
  }
  /** 剥除旧版残留：builtin 分组 / builtin 条目（内置预置已废弃，删除后不复活） */
  stripBuiltins(f) {
    const out = clone(f);
    out.groups = out.groups.filter((g) => !g.builtin);
    out.items = out.items.filter((i) => !i.builtin);
    out.groups = out.groups.map((g) => {
      const { builtin: _b, ...rest } = g;
      return rest;
    });
    out.items = out.items.map((i) => {
      const { builtin: _b, ...rest } = i;
      return rest;
    });
    return out;
  }
  /**
   * 孤儿归位：条目所属分组不存在时（如内置组被剥除后组内残留的用户自建条目），
   * 收进「迁移条目」组。没有孤儿则原样返回。
   */
  homeOrphans(f) {
    const valid = new Set(f.groups.map((g) => g.id));
    const orphans = f.items.filter((i) => !valid.has(i.group));
    if (orphans.length === 0) return f;
    const out = clone(f);
    let migrated = out.groups.find((g) => g.id === "grp-migrated");
    if (!migrated) {
      migrated = { id: "grp-migrated", name: "\u8FC1\u79FB\u6761\u76EE" };
      out.groups.push(migrated);
    }
    orphans.forEach((o) => {
      const item = out.items.find((x) => x.id === o.id);
      if (item) item.group = migrated.id;
    });
    return out;
  }
  /** v1/v2（triggers / slots / itemIds / template / type）→ v4 */
  migrateLegacy(old) {
    var _a;
    const out = this.emptyFormats();
    const keepGroups = [];
    const keepItems = [];
    if (Array.isArray(old.groups)) {
      old.groups.forEach((g) => {
        keepGroups.push({ id: g.id, name: g.name || g.id, builtin: !!g.builtin });
      });
    }
    const memberOf = /* @__PURE__ */ new Map();
    if (Array.isArray(old.groups)) {
      old.groups.forEach((g) => (g.itemIds || []).forEach((iid) => memberOf.set(iid, g.id)));
    }
    if (Array.isArray(old.items)) {
      old.items.forEach((i) => {
        var _a2, _b;
        keepItems.push({
          id: i.id,
          name: i.name || i.id,
          text: (_b = (_a2 = i.text) != null ? _a2 : i.template) != null ? _b : "",
          group: memberOf.get(i.id) || "",
          builtin: !!i.builtin
        });
      });
    }
    out.groups = keepGroups.filter((g) => !g.builtin).map((g) => ({ id: g.id, name: g.name }));
    const groupIds = new Set(out.groups.map((g) => g.id));
    const userItems = keepItems.filter((i) => !i.builtin);
    const keepItemsBound = userItems.filter((i) => i.group && groupIds.has(i.group));
    const orphans = userItems.filter((i) => !i.group || !groupIds.has(i.group));
    if (orphans.length > 0 && !groupIds.has("grp-migrated")) {
      out.groups.push({ id: "grp-migrated", name: "\u8FC1\u79FB\u6761\u76EE" });
      groupIds.add("grp-migrated");
      orphans.forEach((o) => keepItemsBound.push({ ...o, group: "grp-migrated" }));
    }
    out.items = keepItemsBound.map((i) => ({ id: i.id, name: i.name, text: i.text, group: i.group }));
    let chosen = (_a = old.triggers) == null ? void 0 : _a.find((t) => t.id === "trig-at");
    if (!chosen && old.triggers && old.triggers.length) {
      chosen = [...old.triggers].sort(
        (a, b) => (b.groupIds || []).length - (a.groupIds || []).length
      )[0];
    }
    out.triggerChar = chosen && chosen.chars && chosen.chars.length && !/\s/.test(chosen.chars[0]) ? chosen.chars[0] : DEFAULT_TRIGGER_CHAR;
    out.quickCommands = [];
    return out;
  }
  // ============ 查询 / 触发 / 展开 ============
  groupById(id) {
    return this._data.groups.find((g) => g.id === id);
  }
  /** 当前全局触发字符（未设专属触发符的分组共用） */
  get triggerChar() {
    return this._data.triggerChar || DEFAULT_TRIGGER_CHAR;
  }
  /**
   * 光标前文本 → 命中的触发符与它覆盖的分组。
   * - 分组有专属触发符 g.trigger 时只被该触发符弹出；
   * - 未设触发符的分组跟随全局 triggerChar；
   * - 多组可共用同一触发符（命中后合并弹出，靠分组头区分）。
   * 返回 null = 没有命中任何入口（此时不弹菜单）。
   */
  findTriggerMatch(textBefore) {
    if (!textBefore) return null;
    const def = this.triggerChar;
    const candidates = /* @__PURE__ */ new Set([def]);
    this._data.groups.forEach((g) => {
      if (g.trigger) candidates.add(g.trigger);
    });
    let best = null;
    for (const c of candidates) {
      if (!c) continue;
      if (textBefore.endsWith(c) && (!best || c.length > best.length)) best = c;
    }
    if (!best) return null;
    const groupIds = this._data.groups.filter((g) => g.trigger ? g.trigger === best : best === def).map((g) => g.id);
    return groupIds.length > 0 ? { char: best, groupIds } : null;
  }
  /**
   * 触发符集合校验（全局默认 + 各分组专属）：非空、不含空白；
   * 不同值之间禁止互为前缀（输入 `@` 时会吞掉 `@@` 造成歧义）；相同值允许多组共用。
   */
  validateTriggerSet(values) {
    for (const raw of values) {
      if (raw && /\s/.test(raw)) {
        return { ok: false, message: `\u89E6\u53D1\u7B26\u300C${raw.trim() || "(\u7A7A\u683C)"}\u300D\u4E0D\u80FD\u5305\u542B\u7A7A\u767D\u5B57\u7B26` };
      }
    }
    const list = values.filter((c) => !!c && c.length > 0);
    for (let i = 0; i < list.length; i++) {
      for (let j = 0; j < list.length; j++) {
        if (i === j) continue;
        if (list[i] === list[j]) continue;
        if (list[i].startsWith(list[j]) || list[j].startsWith(list[i])) {
          return {
            ok: false,
            message: `\u89E6\u53D1\u7B26\u300C${list[i]}\u300D\u4E0E\u300C${list[j]}\u300D\u4E92\u4E3A\u524D\u7F00\uFF0C\u8F93\u5165\u65F6\u4F1A\u4E92\u76F8\u541E\u5E76`
          };
        }
      }
    }
    return { ok: true, message: "" };
  }
  /** 当前配置全部触发符（默认 + 各组专属） */
  currentTriggerValues() {
    const vals = [this.triggerChar];
    this._data.groups.forEach((g) => {
      if (g.trigger) vals.push(g.trigger);
    });
    return vals;
  }
  /** 全部模板条目按分组顺序展开为弹窗建议（含分组头 group 字段） */
  expandAll() {
    return this.expandGroups(this._data.groups.map((g) => g.id));
  }
  /** 指定分组展开（组顺序按传入；条目按组内数组顺序） */
  expandGroups(groupIds) {
    const out = [];
    const seen = /* @__PURE__ */ new Set();
    const pushGroup = (gid) => {
      const g = this.groupById(gid);
      if (!g) return;
      for (const item of this._data.items) {
        if (item.group !== gid || seen.has(item.id)) continue;
        seen.add(item.id);
        out.push({
          name: item.name,
          display: item.name,
          template: item.text,
          insert: item.text,
          id: item.id,
          group: g.name
        });
      }
    };
    groupIds.forEach(pushGroup);
    return out;
  }
  /** 直达命令配置 */
  quickCommand(slot) {
    return this._data.quickCommands.find((q) => q.slot === slot);
  }
  /** 原子修改：mutator 内直接改传入副本，落盘后刷新内存 */
  async mutate(mutator) {
    const draft = clone(this._data);
    const result = mutator(draft);
    await this.write(draft);
    this._data = draft;
    return result;
  }
  async save() {
    await this.write(this._data);
  }
  async write(f) {
    try {
      const adapter = this.plugin.app.vault.adapter;
      if (!await adapter.exists(FORMATS_DIR)) {
        await adapter.mkdir(FORMATS_DIR).catch(() => {
        });
      }
      await adapter.write(FORMATS_FILE_PATH, JSON.stringify(f, null, 2));
    } catch (e) {
      console.error("[InFlow] formats.json \u5199\u5165\u5931\u8D25:", e);
      new import_obsidian5.Notice("\u6A21\u677F\u914D\u7F6E\u4FDD\u5B58\u5931\u8D25\uFF0C\u8BF7\u68C0\u67E5 .inflow \u76EE\u5F55\u6743\u9650");
    }
  }
  exportJson() {
    return JSON.stringify(this._data, null, 2);
  }
  importJson(raw) {
    try {
      const { file } = this.parse(raw);
      this._data = file;
      void this.save();
      return { ok: true, message: "\u5BFC\u5165\u6210\u529F" };
    } catch (e) {
      return {
        ok: false,
        message: `\u5BFC\u5165\u5931\u8D25\uFF1A${e instanceof Error ? e.message : String(e)}`
      };
    }
  }
  /** 生成不与现有 id 冲突的自定义 id */
  newCustomId(prefix) {
    const taken = /* @__PURE__ */ new Set();
    this._data.items.forEach((i) => taken.add(i.id));
    this._data.groups.forEach((g) => taken.add(g.id));
    let id = "";
    do {
      id = `${prefix}-${Date.now().toString(36)}${Math.floor(Math.random() * 36).toString(36)}`;
    } while (taken.has(id));
    return id;
  }
};

// src/settingsTab.ts
var SimpleTextModal = class extends import_obsidian6.Modal {
  constructor(app, title, placeholder, initial, onConfirm) {
    super(app);
    this.title = title;
    this.placeholder = placeholder;
    this.initial = initial;
    this.onConfirm = onConfirm;
  }
  onOpen() {
    const { contentEl } = this;
    contentEl.empty();
    contentEl.addClass("blfc-plugin");
    contentEl.createEl("h3", { text: this.title });
    let value = this.initial;
    const input = contentEl.createEl("input", {
      type: "text",
      placeholder: this.placeholder,
      value: this.initial,
      cls: "blfc-fmt-name-input"
    });
    input.select();
    const submit = () => {
      const v = value.trim();
      if (!v) {
        new import_obsidian6.Notice("\u540D\u79F0\u4E0D\u80FD\u4E3A\u7A7A");
        return;
      }
      this.onConfirm(v);
      this.close();
    };
    input.addEventListener("keydown", (e) => {
      e.stopPropagation();
      if (e.key === "Enter") submit();
    });
    input.addEventListener("input", () => {
      value = input.value;
    });
    new import_obsidian6.Setting(contentEl).addButton((btn) => btn.setButtonText("\u786E\u5B9A").setCta().onClick(submit)).addButton((btn) => btn.setButtonText("\u53D6\u6D88").onClick(() => this.close()));
  }
  onClose() {
    this.contentEl.empty();
  }
};
var DirectCommandModal = class extends import_obsidian6.Modal {
  constructor(app, groups, onConfirm, defaultSelected = []) {
    super(app);
    this.groups = groups;
    this.onConfirm = onConfirm;
    this.name = "\u63D2\u5165\u6A21\u677F";
    this.picked = new Set(defaultSelected);
  }
  onOpen() {
    const { contentEl } = this;
    contentEl.empty();
    contentEl.addClass("blfc-plugin");
    contentEl.createEl("h3", { text: "\u6DFB\u52A0\u76F4\u8FBE\u547D\u4EE4" });
    contentEl.createEl("p", {
      text: "\u8BE5\u547D\u4EE4\u4F1A\u51FA\u73B0\u5728\u547D\u4EE4\u9762\u677F\u4E0E\u5FEB\u6377\u952E\u8BBE\u7F6E\u4E2D\uFF08\u540D\u79F0\u9700\u91CD\u8F7D\u63D2\u4EF6\u540E\u751F\u6548\uFF09\uFF0C\u76F4\u8FBE\u6240\u9009\u5206\u7EC4\u83DC\u5355\u3002",
      cls: "blfc-fmt-transfer-desc"
    });
    const nameInput = contentEl.createEl("input", {
      type: "text",
      value: this.name,
      cls: "blfc-fmt-name-input"
    });
    nameInput.addEventListener("input", () => {
      this.name = nameInput.value;
    });
    contentEl.createDiv({ text: "\u6253\u5F00\u4EE5\u4E0B\u5206\u7EC4\uFF1A", cls: "blfc-fmt-edit-preview-label" });
    const listEl = contentEl.createDiv({ cls: "blfc-fmt-group-pick" });
    this.groups.forEach((g) => {
      const label = listEl.createEl("label", { cls: "blfc-fmt-group-pick-item" });
      const cb = label.createEl("input", { type: "checkbox" });
      cb.checked = this.picked.has(g.id);
      label.createSpan({ text: g.name });
      cb.addEventListener("change", () => {
        if (cb.checked) this.picked.add(g.id);
        else this.picked.delete(g.id);
      });
    });
    new import_obsidian6.Setting(contentEl).addButton(
      (btn) => btn.setButtonText("\u4FDD\u5B58").setCta().onClick(() => {
        if (!this.name.trim()) {
          new import_obsidian6.Notice("\u547D\u4EE4\u540D\u4E0D\u80FD\u4E3A\u7A7A");
          return;
        }
        if (this.picked.size === 0) {
          new import_obsidian6.Notice("\u81F3\u5C11\u9009\u62E9\u4E00\u4E2A\u5206\u7EC4");
          return;
        }
        this.onConfirm(this.name.trim(), [...this.picked]);
        this.close();
      })
    ).addButton((btn) => btn.setButtonText("\u53D6\u6D88").onClick(() => this.close()));
  }
  onClose() {
    this.contentEl.empty();
  }
};
var ConfirmModal = class extends import_obsidian6.Modal {
  constructor(app, title, message, onConfirm) {
    super(app);
    this.title = title;
    this.message = message;
    this.onConfirm = onConfirm;
  }
  onOpen() {
    const { contentEl } = this;
    contentEl.empty();
    contentEl.addClass("blfc-plugin");
    contentEl.createEl("h3", { text: this.title });
    contentEl.createEl("p", { text: this.message });
    new import_obsidian6.Setting(contentEl).addButton(
      (btn) => btn.setButtonText("\u786E\u8BA4\u5220\u9664").setWarning().onClick(() => {
        this.onConfirm();
        this.close();
      })
    ).addButton((btn) => btn.setButtonText("\u53D6\u6D88").onClick(() => this.close()));
  }
  onClose() {
    this.contentEl.empty();
  }
};
var SimpleScriptSettingTab = class extends import_obsidian6.PluginSettingTab {
  constructor(app, plugin) {
    super(app, plugin);
    this.plugin = plugin;
    /** 模板库设置区块容器（内容全部动态重建） */
    this.formatsArea = null;
    /** 二级页面导航状态：null=主设置页；groupId=正在查看的分组页 */
    this.activeGroupId = null;
    /** 二级页面导航状态：true=正在查看「词库列表」页 */
    this.showLibraryList = false;
    /** 二级页面导航状态：true=正在查看「词库参考」页（词库格式说明） */
    this.showLibraryReference = false;
  }
  display() {
    const { containerEl } = this;
    containerEl.empty();
    containerEl.addClass("blfc-plugin");
    if (this.activeGroupId) {
      this.renderGroupSubpage(this.activeGroupId);
      return;
    }
    if (this.showLibraryList) {
      this.renderLibraryListSubpage();
      return;
    }
    if (this.showLibraryReference) {
      this.renderReferenceSubpage();
      return;
    }
    new import_obsidian6.Setting(containerEl).setName("\u57FA\u672C\u8BBE\u7F6E").setHeading();
    new import_obsidian6.Setting(containerEl).setName("\u542F\u7528\u63D2\u4EF6").setDesc("\u542F\u7528\u6216\u7981\u7528\u683C\u5F0F\u8865\u5168\u529F\u80FD").addToggle(
      (toggle) => toggle.setValue(this.plugin.settings.enabled).onChange(async (value) => {
        this.plugin.settings.enabled = value;
        await this.plugin.saveSettings();
      })
    );
    new import_obsidian6.Setting(containerEl).setName("\u81EA\u52A8\u5BF9\u8BDD\u5EFA\u8BAE").setDesc("\u5728\u89D2\u8272\u540D\u540E\u81EA\u52A8\u5EFA\u8BAE\u63D2\u5165\u5BF9\u8BDD\u683C\u5F0F").addToggle(
      (toggle) => toggle.setValue(this.plugin.settings.autoDialogue).onChange(async (value) => {
        this.plugin.settings.autoDialogue = value;
        await this.plugin.saveSettings();
      })
    );
    new import_obsidian6.Setting(containerEl).setName("\u667A\u80FD\u8865\u5168").setHeading();
    new import_obsidian6.Setting(containerEl).setName("\u542F\u7528\u667A\u80FD\u8865\u5168").setDesc("\u542F\u7528\u667A\u80FD\u8865\u5168\u529F\u80FD").addToggle(
      (toggle) => toggle.setValue(this.plugin.settings.enableSmartCompletion).onChange(async (value) => {
        this.plugin.settings.enableSmartCompletion = value;
        await this.plugin.saveSettings();
      })
    );
    new import_obsidian6.Setting(containerEl).setName("\u542F\u7528\u4E0A\u4E0B\u6587\u611F\u77E5").setDesc("\u6839\u636E\u5149\u6807\u4F4D\u7F6E\u81EA\u52A8\u63D0\u4F9B\u76F8\u5173\u8865\u5168\u5EFA\u8BAE").addToggle(
      (toggle) => toggle.setValue(this.plugin.settings.enableContextAware).onChange(async (value) => {
        this.plugin.settings.enableContextAware = value;
        await this.plugin.saveSettings();
      })
    );
    new import_obsidian6.Setting(containerEl).setName("\u542F\u7528\u6700\u5C0F\u89E6\u53D1").setDesc("\u5728\u7279\u5B9A\u683C\u5F0F\u4F4D\u7F6E\uFF08\u5982\u573A\u666F\u6807\u9898\u3001\u89D2\u8272\u540D\uFF09\u5373\u4F7F\u6CA1\u6709\u8F93\u5165\u4E5F\u663E\u793A\u5EFA\u8BAE").addToggle(
      (toggle) => toggle.setValue(this.plugin.settings.enableMinimalTrigger).onChange(async (value) => {
        this.plugin.settings.enableMinimalTrigger = value;
        await this.plugin.saveSettings();
      })
    );
    new import_obsidian6.Setting(containerEl).setName("\u542F\u7528\u62FC\u97F3\u5339\u914D").setDesc("\u542F\u7528\u62FC\u97F3\u9996\u5B57\u6BCD\u5339\u914D\u529F\u80FD").addToggle(
      (toggle) => toggle.setValue(this.plugin.settings.enablePinyin).onChange(async (value) => {
        this.plugin.settings.enablePinyin = value;
        await this.plugin.saveSettings();
      })
    );
    new import_obsidian6.Setting(containerEl).setName("\u667A\u80FD\u8865\u5168\u6700\u5C0F\u89E6\u53D1\u957F\u5EA6").setDesc("\u8F93\u5165\u591A\u5C11\u4E2A\u5B57\u7B26\u540E\u5F00\u59CB\u667A\u80FD\u8865\u5168\u5EFA\u8BAE").addSlider(
      (slider) => slider.setLimits(1, 5, 1).setValue(this.plugin.settings.smartMinLength).setDynamicTooltip().onChange(async (value) => {
        this.plugin.settings.smartMinLength = value;
        await this.plugin.saveSettings();
      })
    );
    new import_obsidian6.Setting(containerEl).setName("\u667A\u80FD\u8865\u5168\u6700\u5927\u5EFA\u8BAE\u6570").setDesc("\u667A\u80FD\u8865\u5168\u6700\u591A\u663E\u793A\u591A\u5C11\u4E2A\u5EFA\u8BAE").addSlider(
      (slider) => slider.setLimits(5, 20, 1).setValue(this.plugin.settings.smartMaxSuggestions).setDynamicTooltip().onChange(async (value) => {
        this.plugin.settings.smartMaxSuggestions = value;
        await this.plugin.saveSettings();
      })
    );
    new import_obsidian6.Setting(containerEl).setName("\u7EC4\u5408\u5EFA\u8BAE").setHeading();
    new import_obsidian6.Setting(containerEl).setName("\u542F\u7528\u573A\u666F/\u5BF9\u8BDD\u7EC4\u5408\u5EFA\u8BAE").setDesc("\u751F\u6210\u300C\u573A\u666F\xD7\u65F6\u95F4\u300D\u300C\u89D2\u8272\xD7\u53F0\u8BCD\u300D\u7684\u7EC4\u5408\u8BCD\u6761\uFF1B\u8BCD\u5E93\u8F83\u5927\u65F6\u7EC4\u5408\u9879\u53EF\u80FD\u6DF9\u6CA1\u771F\u5B9E\u8BCD\u6761\uFF0C\u53EF\u5173\u95ED").addToggle(
      (toggle) => toggle.setValue(this.plugin.settings.enableCombos).onChange(async (value) => {
        this.plugin.settings.enableCombos = value;
        await this.plugin.saveSettings();
      })
    );
    new import_obsidian6.Setting(containerEl).setName("\u7EC4\u5408\u5EFA\u8BAE\u6700\u5927\u6761\u6570").setDesc("\u7EC4\u5408\u8BCD\u6761\u7684\u4E0A\u9650\uFF0C\u8D85\u51FA\u90E8\u5206\u81EA\u52A8\u622A\u65AD\uFF08\u4EC5\u542F\u7528\u7EC4\u5408\u5EFA\u8BAE\u65F6\u751F\u6548\uFF09").addSlider(
      (slider) => slider.setLimits(20, 200, 10).setValue(this.plugin.settings.comboMaxItems).setDynamicTooltip().onChange(async (value) => {
        this.plugin.settings.comboMaxItems = value;
        await this.plugin.saveSettings();
      })
    );
    new import_obsidian6.Setting(containerEl).setName("\u5FEB\u6377\u60AC\u6D6E\u9762\u677F").setHeading();
    new import_obsidian6.Setting(containerEl).setName("\u542F\u7528\u5FEB\u6377\u60AC\u6D6E\u9762\u677F").setDesc("\u5728\u7F16\u8F91\u5668\u754C\u9762\u663E\u793A\u8BCD\u5E93\u5FEB\u6377\u64CD\u4F5C\u9762\u677F").addToggle(
      (toggle) => toggle.setValue(this.plugin.settings.enableQuickPanel).onChange(async (value) => {
        this.plugin.settings.enableQuickPanel = value;
        await this.plugin.saveSettings();
        if (value && !this.plugin.quickPanel) {
          this.plugin.quickPanel = new LibraryEdgeStrips(this.plugin);
          this.plugin.quickPanel.create();
        } else if (!value && this.plugin.quickPanel) {
          this.plugin.quickPanel.destroy();
          this.plugin.quickPanel = null;
        }
      })
    );
    new import_obsidian6.Setting(containerEl).setName("\u8BCD\u5E93\u66F4\u65B0\u81EA\u52A8\u5237\u65B0").setDesc("\u4FEE\u6539\u8BCD\u5E93\u6587\u4EF6\u540E\u81EA\u52A8\u5237\u65B0\u5F53\u524D\u8BCD\u5E93\uFF08\u65E0\u9700\u624B\u52A8\u64CD\u4F5C\uFF09").addToggle(
      (toggle) => toggle.setValue(this.plugin.settings.enableAutoRefresh).onChange(async (value) => {
        this.plugin.settings.enableAutoRefresh = value;
        await this.plugin.saveSettings();
      })
    );
    new import_obsidian6.Setting(containerEl).setName("\u81EA\u52A8\u5237\u65B0\u540E\u663E\u793A\u901A\u77E5").setDesc("\u8BCD\u5E93\u81EA\u52A8\u5237\u65B0\u540E\u663E\u793A\u7B80\u77ED\u901A\u77E5").addToggle(
      (toggle) => toggle.setValue(this.plugin.settings.showAutoRefreshNotice).onChange(async (value) => {
        this.plugin.settings.showAutoRefreshNotice = value;
        await this.plugin.saveSettings();
      })
    );
    this.formatsArea = containerEl.createDiv({ cls: "blfc-fmt-area" });
    this.renderFormatsArea();
    this.renderLibraryOverview();
    this.renderReferenceOverview();
    new import_obsidian6.Setting(containerEl).setName("\u8C03\u8BD5").setHeading();
    new import_obsidian6.Setting(containerEl).setName("\u6D4B\u8BD5\u529F\u80FD").setDesc("\u6D4B\u8BD5\u5F53\u524D\u8BCD\u5E93\u52A0\u8F7D\u60C5\u51B5").addButton(
      (button) => button.setButtonText("\u6D4B\u8BD5").onClick(async () => {
        const libraryDir = this.plugin.libraryManager.getLibraryDirectory();
        const libraries = this.plugin.libraryManager.getAvailableLibraries();
        const activeLibrary = this.plugin.libraryManager.activeLibrary;
        let message = "";
        if (libraryDir) {
          message += `\u8BCD\u5E93\u6587\u4EF6\u5939: ${libraryDir}
`;
          message += `\u627E\u5230 ${libraries.length} \u4E2A\u8BCD\u5E93
`;
          if (activeLibrary) {
            const info = this.plugin.libraryManager.getLibraryInfo(activeLibrary);
            message += `\u5F53\u524D\u8BCD\u5E93: ${activeLibrary}
`;
            if (info) {
              message += `\u603B\u8BCD\u6761\u6570: ${info.itemCount}\u4E2A
`;
              if (info.metadata && Object.keys(info.metadata).length > 0) {
                message += `\u5143\u6570\u636E: ${JSON.stringify(info.metadata)}
`;
              }
            }
          } else {
            message += "\u5F53\u524D\u672A\u4F7F\u7528\u8BCD\u5E93";
          }
        } else {
          message = "\u8BF7\u5148\u8BBE\u7F6E\u8BCD\u5E93\u6587\u4EF6\u5939";
        }
        new import_obsidian6.Notice(message);
      })
    );
  }
  // ========== 模板库设置（主界面概览 + 分组管理进入二级） ==========
  /** 重建主设置区（改动后调用；分组内部管理在 GroupManageModal 二级界面） */
  renderFormatsArea() {
    if (!this.formatsArea) return;
    const area = this.formatsArea;
    area.empty();
    const fm = this.plugin.formatsManager;
    const file = fm.file;
    const header = area.createDiv({ cls: "blfc-fmt-header" });
    header.createSpan({ text: "\u683C\u5F0F\u6A21\u677F", cls: "blfc-fmt-title" });
    const addBtn = header.createEl("button", {
      text: "\u65B0\u589E",
      cls: "blfc-fmt-add-btn"
    });
    addBtn.addEventListener("click", () => {
      new SimpleTextModal(
        this.app,
        "\u65B0\u5EFA\u5206\u7EC4",
        "\u5206\u7EC4\u540D\uFF0C\u5982\uFF1A\u5468\u62A5\u6A21\u677F",
        "",
        (name) => {
          const g = { id: fm.newCustomId("grp"), name };
          void fm.mutate((d) => {
            d.groups.push(g);
            return null;
          });
          this.renderFormatsArea();
        }
      ).open();
    });
    const entryRow = area.createDiv({ cls: "blfc-fmt-entry" });
    entryRow.createEl("label", { text: "\u9ED8\u8BA4\u89E6\u53D1\u7B26", cls: "blfc-fmt-entry-label" });
    const chInput = entryRow.createEl("input", {
      type: "text",
      value: fm.triggerChar,
      placeholder: "@",
      cls: "blfc-fmt-trigger-input",
      title: "\u672A\u8BBE\u4E13\u5C5E\u89E6\u53D1\u7B26\u7684\u5206\u7EC4\u7531\u5B83\u5F39\u51FA\uFF1B\u652F\u6301\u591A\u5B57\u7B26\uFF08\u5982 ##\uFF09"
    });
    chInput.addEventListener("keydown", (e) => {
      e.stopPropagation();
      if (e.key === "Enter") this.commitTriggerChar(chInput.value);
    });
    chInput.addEventListener("blur", () => this.commitTriggerChar(chInput.value));
    entryRow.createSpan({
      text: "\u8F93\u5165\u540E\u5F39\u51FA\u6A21\u677F\u83DC\u5355\uFF1B\u53EF\u4E3A\u67D0\u5206\u7EC4\u5355\u72EC\u8BBE\u4E13\u5C5E\u89E6\u53D1\u7B26\uFF08\u5728\u5176\u7BA1\u7406\u754C\u9762\u91CC\uFF09\u3002",
      cls: "blfc-fmt-hint"
    });
    if (file.groups.length === 0) {
      area.createDiv({ text: "\u6682\u65E0\u5206\u7EC4\uFF0C\u70B9\u53F3\u4E0A\u89D2\u300C\u65B0\u589E\u300D\u5F00\u59CB\u3002", cls: "blfc-fmt-empty" });
    } else {
      file.groups.forEach((g) => this.renderGroupRow(area, g.id));
    }
    const adv = area.createEl("details", { cls: "blfc-fmt-advanced" });
    adv.createEl("summary", { text: "\u9AD8\u7EA7\uFF1A\u5BFC\u51FA / \u5BFC\u5165" });
    const advRow = adv.createDiv({ cls: "blfc-fmt-tools" });
    const mkAdv = (text, onClick) => {
      const b = advRow.createEl("button", { text, cls: "blfc-fmt-btn" });
      b.addEventListener("click", onClick);
      return b;
    };
    mkAdv("\u5BFC\u51FA\u914D\u7F6E\u2026", () => new FormatsTransferModal(this.app, this.plugin, "export").open());
    mkAdv("\u5BFC\u5165\u914D\u7F6E\u2026", () => new FormatsTransferModal(this.app, this.plugin, "import").open());
  }
  /** 分组一行概览：名称 / 条数+触发命令 / 进入箭头（仅箭头可点击） */
  renderGroupRow(area, groupId) {
    var _a;
    const fm = this.plugin.formatsManager;
    const group = fm.groupById(groupId);
    if (!group) return;
    const count = fm.file.items.filter((i) => i.group === groupId).length;
    const row = area.createDiv({ cls: "blfc-fmt-group-row" });
    const main = row.createDiv({ cls: "blfc-fmt-group-row-main" });
    main.createSpan({ text: group.name, cls: "blfc-fmt-group-row-name" });
    main.createSpan({
      text: `${count}\u6761 \xB7 \u89E6\u53D1 ${(_a = group.trigger) != null ? _a : fm.triggerChar}`,
      cls: "blfc-fmt-group-row-meta"
    });
    const arrowBtn = row.createEl("button", {
      text: "\u203A",
      title: "\u8FDB\u5165\u5206\u7EC4\u8BBE\u7F6E",
      cls: "blfc-fmt-arrow"
    });
    arrowBtn.addEventListener("click", () => this.goToGroup(groupId));
  }
  // ========== 分组二级页面（设置页内页面导航，非弹窗） ==========
  /** 进入分组设置页 */
  goToGroup(groupId) {
    this.activeGroupId = groupId;
    this.display();
  }
  /** 返回主设置页 */
  backToOverview() {
    this.activeGroupId = null;
    this.showLibraryList = false;
    this.showLibraryReference = false;
    this.display();
  }
  /** 进入词库列表二级页 */
  goToLibraryList() {
    this.showLibraryList = true;
    this.display();
  }
  /** 进入「词库参考」二级页（词库格式说明） */
  goToReference() {
    this.showLibraryReference = true;
    this.display();
  }
  /** 渲染分组设置二级页面（替换整页内容） */
  renderGroupSubpage(groupId) {
    const fm = this.plugin.formatsManager;
    const group = fm.groupById(groupId);
    if (!group) {
      this.backToOverview();
      return;
    }
    const { containerEl } = this;
    containerEl.empty();
    containerEl.addClass("blfc-plugin");
    const nav = containerEl.createDiv({ cls: "blfc-fmt-subnav" });
    const backBtn = nav.createEl("button", { text: "\u8FD4\u56DE", cls: "blfc-fmt-btn" });
    backBtn.addEventListener("click", () => this.backToOverview());
    nav.createSpan({ text: "\u683C\u5F0F\u6A21\u677F", cls: "blfc-fmt-subnav-crumb" });
    const groupTitle = new import_obsidian6.Setting(containerEl).setName(`${group.name} \xB7 \u5206\u7EC4\u8BBE\u7F6E`).setHeading();
    const triggerRow = containerEl.createDiv({ cls: "blfc-fmt-entry" });
    triggerRow.createEl("label", { text: "\u4E13\u5C5E\u89E6\u53D1\u7B26", cls: "blfc-fmt-entry-label" });
    const trigInput = triggerRow.createEl("input", {
      type: "text",
      value: group.trigger || "",
      placeholder: `\u7559\u7A7A\uFF1D\u8DDF\u968F\u9ED8\u8BA4 ${fm.triggerChar}`,
      cls: "blfc-fmt-trigger-input",
      title: "\u7559\u7A7A\u5219\u7528\u5168\u5C40\u9ED8\u8BA4\u89E6\u53D1\u7B26\u5F39\u51FA\u672C\u7EC4\uFF1B\u586B\u4E86\u5982 # \u540E\uFF0C\u8F93\u5165 # \u624D\u5F39\u672C\u7EC4"
    });
    const commitTrig = (raw) => {
      const v = raw.trim();
      if (!v || v === fm.triggerChar) {
        void fm.mutate((d) => {
          const g = d.groups.find((x) => x.id === groupId);
          if (g) delete g.trigger;
          return null;
        });
        trigInput.value = "";
        new import_obsidian6.Notice("\u5DF2\u6062\u590D\u8DDF\u968F\u5168\u5C40\u9ED8\u8BA4\u89E6\u53D1\u7B26");
        return;
      }
      const others = fm.currentTriggerValues().filter((c) => !(group.trigger && c === group.trigger) && c !== v);
      const check = fm.validateTriggerSet([...others, v]);
      if (!check.ok) {
        new import_obsidian6.Notice(check.message);
        return;
      }
      void fm.mutate((d) => {
        const g = d.groups.find((x) => x.id === groupId);
        if (g) g.trigger = v;
        return null;
      });
      trigInput.value = v;
      new import_obsidian6.Notice(`\u5DF2\u8BBE\u4E3A\u4E13\u5C5E\u89E6\u53D1\u7B26\uFF1A${v}`);
    };
    trigInput.addEventListener("keydown", (e) => {
      e.stopPropagation();
      if (e.key === "Enter") commitTrig(trigInput.value);
    });
    trigInput.addEventListener("blur", () => commitTrig(trigInput.value));
    containerEl.createDiv({
      text: "\u7A7A = \u8DDF\u968F\u5168\u5C40\u9ED8\u8BA4\uFF1B\u591A\u7EC4\u53EF\u7528\u540C\u4E00\u89E6\u53D1\u7B26\uFF08\u5408\u5E76\u5F39\u51FA\uFF09\uFF1B\u4E0D\u540C\u89E6\u53D1\u7B26\u7981\u6B62\u4E92\u4E3A\u524D\u7F00\uFF08\u5982 @ \u4E0E @@\uFF09\u3002",
      cls: "blfc-fmt-hint"
    });
    const renameBtn = containerEl.createEl("button", {
      text: "\u91CD\u547D\u540D\u5206\u7EC4\u2026",
      cls: "blfc-fmt-btn blfc-fmt-btn-ren"
    });
    renameBtn.addEventListener("click", () => {
      new SimpleTextModal(this.app, "\u91CD\u547D\u540D\u5206\u7EC4", "\u5206\u7EC4\u540D", group.name, (name) => {
        void fm.mutate((d) => {
          const g = d.groups.find((x) => x.id === groupId);
          if (g) g.name = name;
          return null;
        });
        groupTitle.setName(`${name.trim()} \xB7 \u5206\u7EC4\u8BBE\u7F6E`);
      }).open();
    });
    containerEl.createDiv({ cls: "blfc-fmt-subhead" }).textContent = "\u76F4\u8FBE\u547D\u4EE4";
    containerEl.createDiv({
      text: "\u52FE\u9009 = \u672C\u7EC4\u7531\u8BE5\u547D\u4EE4\u5F39\u51FA\u3002\u547D\u4EE4\u53EF\u7ED1\u5B9A\u591A\u4E2A\u5206\u7EC4\uFF1B\u9700\u91CD\u8F7D\u63D2\u4EF6\u540E\u547D\u4EE4\u624D\u51FA\u73B0\u5728\u547D\u4EE4\u9762\u677F / \u5FEB\u6377\u952E\u91CC\u3002",
      cls: "blfc-fmt-hint"
    });
    const quickBox = containerEl.createDiv({ cls: "blfc-fmt-qc-box" });
    const reRenderQuick = () => {
      quickBox.empty();
      const qcs = [...fm.file.quickCommands].sort((a, b) => a.slot - b.slot);
      if (qcs.length === 0) {
        quickBox.createDiv({ text: "\u8FD8\u6CA1\u6709\u76F4\u8FBE\u547D\u4EE4\uFF0C\u70B9\u4E0B\u65B9\u300C+ \u6DFB\u52A0\u300D\u521B\u5EFA\u3002", cls: "blfc-fmt-empty" });
      }
      qcs.forEach((q) => {
        const row = quickBox.createEl("label", { cls: "blfc-fmt-qc-row" });
        const cb = row.createEl("input", { type: "checkbox" });
        cb.checked = q.groupIds.includes(groupId);
        cb.addEventListener("change", () => {
          void fm.mutate((d) => {
            const qq = d.quickCommands.find((x) => x.slot === q.slot);
            if (!qq) return null;
            if (cb.checked) {
              if (!qq.groupIds.includes(groupId)) qq.groupIds.push(groupId);
            } else {
              qq.groupIds = qq.groupIds.filter((x) => x !== groupId);
            }
            return null;
          });
          new import_obsidian6.Notice(cb.checked ? "\u5DF2\u7ED1\u5B9A\u672C\u7EC4" : "\u5DF2\u89E3\u9664\u672C\u7EC4");
          reRenderQuick();
        });
        row.createSpan({ text: `\u76F4\u8FBE ${q.slot} \xB7 ${q.name}`, cls: "blfc-fmt-qc-name" });
        const bound = q.groupIds.length;
        row.createSpan({ text: `\u5DF2\u7ED1 ${bound} \u7EC4`, cls: "blfc-fmt-count" });
        const del = row.createEl("button", { text: "\u2715", title: "\u5220\u9664\u8BE5\u76F4\u8FBE\u547D\u4EE4\uFF08\u89E3\u9664\u6240\u6709\u5206\u7EC4\u7ED1\u5B9A\uFF09", cls: "blfc-fmt-btn" });
        del.addEventListener("click", (ev) => {
          var _a, _b;
          ev.preventDefault();
          const boundNow = (_b = (_a = fm.file.quickCommands.find((x) => x.slot === q.slot)) == null ? void 0 : _a.groupIds.length) != null ? _b : 0;
          new ConfirmModal(
            this.app,
            "\u5220\u9664\u76F4\u8FBE\u547D\u4EE4\uFF1F",
            `\u5C06\u5220\u9664\u300C\u76F4\u8FBE ${q.slot} \xB7 ${q.name}\u300D\uFF0C\u5176\u7ED1\u5B9A\u7684 ${boundNow} \u4E2A\u5206\u7EC4\u4E00\u5E76\u89E3\u9664\u3002`,
            () => {
              void fm.mutate((d) => {
                d.quickCommands = d.quickCommands.filter((x) => x.slot !== q.slot);
                return null;
              });
              reRenderQuick();
            }
          ).open();
        });
      });
      const addQuick = quickBox.createEl("button", {
        text: "+ \u6DFB\u52A0\u76F4\u8FBE\u547D\u4EE4",
        cls: "blfc-fmt-btn"
      });
      addQuick.addEventListener("click", () => {
        if (fm.file.quickCommands.length >= MAX_QUICK_COMMANDS) {
          new import_obsidian6.Notice(`\u76F4\u8FBE\u547D\u4EE4\u6700\u591A ${MAX_QUICK_COMMANDS} \u6761`);
          return;
        }
        new DirectCommandModal(
          this.app,
          fm.file.groups.map((g) => ({ id: g.id, name: g.name })),
          (name, groupIds) => {
            const slot = this.nextFreeSlot(fm.file);
            if (!slot) {
              new import_obsidian6.Notice(`\u76F4\u8FBE\u547D\u4EE4\u6700\u591A ${MAX_QUICK_COMMANDS} \u6761`);
              return;
            }
            void fm.mutate((d) => {
              d.quickCommands.push({ slot, name, groupIds });
              return null;
            });
            new import_obsidian6.Notice("\u5DF2\u6DFB\u52A0\uFF08\u547D\u4EE4\u540D\u79F0\u9700\u91CD\u8F7D\u63D2\u4EF6\u540E\u663E\u793A\uFF09");
            reRenderQuick();
          },
          [groupId]
        ).open();
      });
    };
    reRenderQuick();
    containerEl.createDiv({ cls: "blfc-fmt-subhead" }).textContent = "\u6A21\u677F\u6761\u76EE";
    const itemsBox = containerEl.createDiv({ cls: "blfc-fmt-items" });
    const addBtn = containerEl.createEl("button", {
      text: "+ \u6DFB\u52A0\u6A21\u677F\u5230\u672C\u7EC4",
      cls: "blfc-fmt-btn blfc-fmt-btn-add"
    });
    const reRenderItems = () => {
      itemsBox.empty();
      const items = fm.file.items.filter((i) => i.group === groupId);
      if (items.length === 0) {
        itemsBox.createDiv({ text: "\uFF08\u7A7A\u7EC4\uFF09", cls: "blfc-fmt-empty" });
      }
      items.forEach((item, rowIdx) => {
        const row = itemsBox.createDiv({ cls: "blfc-fmt-item-row" });
        const nameBtn = row.createEl("button", {
          text: item.name,
          title: "\u70B9\u51FB\u7F16\u8F91",
          cls: "blfc-fmt-item-name"
        });
        const openEdit = () => {
          new FormatItemEditModal(this.app, this.plugin, { ...item }, (saved) => {
            void fm.mutate((d) => {
              const idx = d.items.findIndex((i) => i.id === saved.id);
              if (idx >= 0) d.items[idx] = saved;
              return null;
            });
            reRenderItems();
          }).open();
        };
        nameBtn.addEventListener("click", openEdit);
        const text = item.text;
        row.createSpan({
          text: text.length > 52 ? `${text.slice(0, 52)}\u2026` : text,
          title: text,
          cls: "blfc-fmt-item-tpl"
        });
        const mk = (t, title, onClick) => {
          const b = row.createEl("button", { text: t, title, cls: "blfc-fmt-btn" });
          b.addEventListener("click", onClick);
        };
        mk("\u2191", "\u4E0A\u79FB\uFF08\u7EC4\u5185\u987A\u5E8F\uFF09", () => {
          void fm.mutate((d) => {
            const arr = d.items.filter((i) => i.group === groupId);
            const idx = arr.findIndex((i) => i.id === item.id);
            if (idx <= 0) return null;
            const a = arr[idx - 1];
            const b = arr[idx];
            const i1 = d.items.indexOf(a);
            const i2 = d.items.indexOf(b);
            if (i1 >= 0 && i2 >= 0) [d.items[i1], d.items[i2]] = [d.items[i2], d.items[i1]];
            return null;
          });
          reRenderItems();
        });
        mk("\u2193", "\u4E0B\u79FB\uFF08\u7EC4\u5185\u987A\u5E8F\uFF09", () => {
          void fm.mutate((d) => {
            const arr = d.items.filter((i) => i.group === groupId);
            const idx = arr.findIndex((i) => i.id === item.id);
            if (idx < 0 || idx >= arr.length - 1) return null;
            const a = arr[idx];
            const b = arr[idx + 1];
            const i1 = d.items.indexOf(a);
            const i2 = d.items.indexOf(b);
            if (i1 >= 0 && i2 >= 0) [d.items[i1], d.items[i2]] = [d.items[i2], d.items[i1]];
            return null;
          });
          reRenderItems();
        });
        mk("\u2715", "\u5220\u9664\u8BE5\u6A21\u677F\uFF08\u5220\u9664\u540E\u4E0D\u53EF\u6062\u590D\uFF09", () => {
          void fm.mutate((d) => {
            d.items = d.items.filter((i) => i.id !== item.id);
            return null;
          });
          reRenderItems();
        });
      });
    };
    reRenderItems();
    addBtn.addEventListener("click", () => {
      const item = {
        id: fm.newCustomId("tpl"),
        name: "\u65B0\u6A21\u677F",
        text: "$0",
        group: groupId
      };
      void fm.mutate((d) => {
        d.items.push(item);
        return null;
      });
      new FormatItemEditModal(this.app, this.plugin, item, (saved) => {
        void fm.mutate((d) => {
          const idx = d.items.findIndex((i) => i.id === saved.id);
          if (idx >= 0) d.items[idx] = saved;
          return null;
        });
        reRenderItems();
      }).open();
    });
    const delBtn = containerEl.createEl("button", {
      text: "\u5220\u9664\u8BE5\u5206\u7EC4\u53CA\u7EC4\u5185\u6A21\u677F",
      cls: "blfc-fmt-btn blfc-fmt-btn-danger"
    });
    delBtn.addEventListener("click", () => {
      const count = fm.file.items.filter((i) => i.group === groupId).length;
      new ConfirmModal(
        this.app,
        "\u5220\u9664\u5206\u7EC4\uFF1F",
        `\u5C06\u5220\u9664\u5206\u7EC4\u300C${group.name}\u300D\u53CA\u5176 ${count} \u6761\u6A21\u677F\u3002\u5220\u9664\u540E\u4E0D\u53EF\u6062\u590D\uFF0C\u8BF7\u786E\u8BA4\u3002`,
        () => {
          void fm.mutate((d) => {
            d.groups = d.groups.filter((g) => g.id !== groupId);
            d.items = d.items.filter((i) => i.group !== groupId);
            d.quickCommands.forEach((q) => {
              q.groupIds = q.groupIds.filter((x) => x !== groupId);
            });
            return null;
          });
          this.backToOverview();
        }
      ).open();
    });
  }
  nextFreeSlot(file) {
    const used = new Set(file.quickCommands.map((q) => q.slot));
    for (let s = 1; s <= MAX_QUICK_COMMANDS; s++) {
      if (!used.has(s)) return s;
    }
    return void 0;
  }
  /** 修改全局默认触发符：与所有分组专属触发符一起做前缀冲突校验 */
  commitTriggerChar(value) {
    const ch = value.trim();
    if (!ch) {
      new import_obsidian6.Notice("\u9ED8\u8BA4\u89E6\u53D1\u7B26\u4E0D\u80FD\u4E3A\u7A7A");
      this.renderFormatsArea();
      return;
    }
    const others = this.plugin.formatsManager.currentTriggerValues().filter(
      (c) => c !== this.plugin.formatsManager.triggerChar
    );
    const check = this.plugin.formatsManager.validateTriggerSet([...others, ch]);
    if (!check.ok) {
      new import_obsidian6.Notice(check.message);
      this.renderFormatsArea();
      return;
    }
    void this.plugin.formatsManager.mutate((d) => {
      d.triggerChar = ch;
      return null;
    });
    this.renderFormatsArea();
  }
  // ========== 词库参考：主区入口卡片 + 二级页（词库格式说明） ==========
  /** 主区渲染：词库参考卡片——标题栏（词库格式说明入口）+ 一句话描述 */
  renderReferenceOverview() {
    const { containerEl } = this;
    const area = containerEl.createDiv({ cls: "blfc-lib-area" });
    const header = area.createDiv({ cls: "blfc-lib-header" });
    header.createSpan({ text: "\u8BCD\u5E93\u53C2\u8003", cls: "blfc-lib-title" });
    const viewBtn = header.createEl("button", {
      text: "\u67E5\u770B \u203A",
      title: "\u8BCD\u5E93\u6587\u4EF6\u7684\u683C\u5F0F\u8BF4\u660E",
      cls: "blfc-lib-entry-btn"
    });
    viewBtn.addEventListener("click", () => this.goToReference());
    const descRow = area.createDiv({ cls: "blfc-fmt-entry" });
    descRow.createSpan({
      text: "\u8BCD\u5E93\u6587\u4EF6\uFF08.md\uFF09\u7684\u4E66\u5199\u683C\u5F0F\uFF1A\u7AE0\u8282\u6807\u9898\u3001\u8BCD\u6761\u5206\u9694\uFF08\u663E\u793A|\u63D2\u5165|\u63CF\u8FF0\uFF09\u3001\u5217\u8868\u6807\u8BB0\u4E0E YAML \u5143\u6570\u636E\u3002",
      cls: "blfc-fmt-hint"
    });
  }
  /** 渲染「词库参考」二级页面：仅保留词库 .md 文件格式说明（替换整页内容） */
  renderReferenceSubpage() {
    const { containerEl } = this;
    containerEl.empty();
    containerEl.addClass("blfc-plugin");
    const nav = containerEl.createDiv({ cls: "blfc-fmt-subnav" });
    const backBtn = nav.createEl("button", { text: "\u8FD4\u56DE", cls: "blfc-fmt-btn" });
    backBtn.addEventListener("click", () => this.backToOverview());
    nav.createSpan({ text: "\u8BCD\u5E93\u53C2\u8003", cls: "blfc-fmt-subnav-crumb" });
    nav.createSpan({ text: "\u203A \u8BCD\u5E93\u683C\u5F0F\u8BF4\u660E", cls: "blfc-fmt-subnav-crumb" });
    new import_obsidian6.Setting(containerEl).setName("\u8BCD\u5E93\u683C\u5F0F\u8BF4\u660E").setHeading();
    containerEl.createDiv({
      text: "\u8BCD\u5E93\u662F\u4E00\u4E2A\u666E\u901A Markdown \u6587\u4EF6\uFF1A\u4E00\u7EA7\u6807\u9898\u4E3A\u8BCD\u5E93\u540D\uFF0C\u4E8C\u7EA7\u6807\u9898\u4E3A\u7C7B\u522B\uFF08\u53EF\u81EA\u7531\u547D\u540D\uFF09\uFF0C\u7C7B\u522B\u4E0B\u9010\u884C\u5199\u8BCD\u6761\u3002",
      cls: "blfc-fmt-hint"
    });
    const doc = containerEl.createDiv({ cls: "blfc-ref-doc" });
    new import_obsidian6.Setting(doc).setName("\u8BCD\u5E93\u6587\u4EF6\u683C\u5F0F\uFF1A").setHeading();
    doc.createEl("pre").createEl("code", {
      text: `# \u6211\u7684\u8BCD\u5E93

## \u89D2\u8272
\u4E3B\u89D2|\u6797\u98CE|\u666E\u901A\u5C11\u5E74\uFF0C\u610F\u5916\u83B7\u5F97\u4FEE\u4ED9\u4F20\u627F
\u5E08\u5C0A|\u7384\u5929\u771F\u4EBA|\u5143\u5A74\u671F\u4FEE\u58EB\uFF0C\u4E25\u5389\u4F46\u62A4\u77ED

## \u5E38\u7528\u8BCD
\u5929\u6750\u5730\u5B9D|\u5929\u6750\u5730\u5B9D|\u73CD\u8D35\u7684\u4FEE\u70BC\u8D44\u6E90
\u673A\u7F18\u5DE7\u5408|\u673A\u7F18\u5DE7\u5408|\u610F\u5916\u7684\u673A\u9047

## \u82F1\u8BED\u5355\u8BCD
happy|happy|\u5FEB\u4E50\u7684
sad|sad|\u60B2\u4F24\u7684`
    });
    new import_obsidian6.Setting(doc).setName("\u683C\u5F0F\u8BF4\u660E\uFF1A").setHeading();
    const ul = doc.createEl("ul");
    const addRich = (parts) => {
      const li = ul.createEl("li");
      parts.forEach((p) => {
        if (typeof p === "string") li.append(p);
        else li.createEl(p.tag, { text: p.text });
      });
    };
    addRich([{ tag: "strong", text: "\u7B80\u5355\u683C\u5F0F\uFF1A" }, { tag: "code", text: "\u8BCD\u6761" }, "\uFF08\u663E\u793A\u4E0E\u63D2\u5165\u6587\u672C\u76F8\u540C\uFF09"]);
    addRich([{ tag: "strong", text: "\u589E\u5F3A\u683C\u5F0F\uFF1A" }, { tag: "code", text: "\u663E\u793A\u6587\u672C|\u63D2\u5165\u6587\u672C" }]);
    addRich([{ tag: "strong", text: "\u5E26\u63CF\u8FF0\uFF1A" }, { tag: "code", text: "\u663E\u793A\u6587\u672C|\u63D2\u5165\u6587\u672C|\u63CF\u8FF0" }, "\uFF08\u63CF\u8FF0\u53EF\u9009\uFF0C\u4EC5\u63D0\u793A\u7528\uFF09"]);
    addRich(["\u652F\u6301\u5217\u8868\u6807\u8BB0\uFF08", { tag: "code", text: "-" }, " \u6216 ", { tag: "code", text: "*" }, "\uFF09\u5F00\u5934"]);
    addRich(["\u652F\u6301 YAML \u5143\u6570\u636E\uFF08\u6587\u4EF6\u5F00\u5934\u7528 ", { tag: "code", text: "---" }, " \u5305\u88F9\uFF09"]);
    addRich(["\u7C7B\u522B\uFF08", { tag: "code", text: "##" }, "\uFF09\u53EF\u81EA\u7531\u547D\u540D\uFF1B", { tag: "code", text: "###" }, " \u4E09\u7EA7\u6807\u9898\u5F52\u5165\u6700\u8FD1\u4E00\u4E2A\u4E8C\u7EA7\u7C7B\u522B\uFF0C\u4E0D\u53E6\u8D77\u7C7B\u522B"]);
  }
  // ========== 词库管理：主区（配置卡片）+ 二级页（词库列表） ==========
  /** 主区渲染：词库管理卡片——标题栏（词库列表入口）+ 词库文件夹路径设置（含刷新按钮） */
  renderLibraryOverview() {
    const { containerEl } = this;
    const area = containerEl.createDiv({ cls: "blfc-lib-area" });
    const header = area.createDiv({ cls: "blfc-lib-header" });
    header.createSpan({ text: "\u8BCD\u5E93\u7BA1\u7406", cls: "blfc-lib-title" });
    const listBtn = header.createEl("button", {
      text: "\u8BCD\u5E93\u5217\u8868 \u203A",
      title: "\u67E5\u770B\u5168\u90E8\u8BCD\u5E93\u5E76\u5207\u6362\u5F53\u524D\u8BCD\u5E93",
      cls: "blfc-lib-entry-btn"
    });
    listBtn.addEventListener("click", () => this.goToLibraryList());
    const folderItem = area.createDiv({ cls: "blfc-lib-folder-line" });
    folderItem.createEl("label", { text: "\u8BCD\u5E93\u6587\u4EF6\u5939", cls: "blfc-lib-config-label" });
    const folderInput = folderItem.createEl("input", {
      type: "text",
      placeholder: "\u4F8B\u5982: \u6211\u7684\u5267\u672C\u5E93 \u6216 /\u6211\u7684\u5267\u672C/\u8BCD\u5E93",
      cls: "blfc-lib-folder-input"
    });
    folderInput.value = this.plugin.settings.libraryFolder || "";
    folderInput.addEventListener("change", () => {
      this.plugin.settings.libraryFolder = folderInput.value.trim();
      void (async () => {
        await this.plugin.saveSettings();
        await this.plugin.libraryManager.loadLibraries();
        this.renderLibraryTable();
      })();
    });
    const refreshBtn = folderItem.createEl("button", {
      text: "\u5237\u65B0",
      cls: "blfc-lib-refresh-btn"
    });
    refreshBtn.addEventListener("click", () => {
      void (async () => {
        await this.plugin.libraryManager.reloadLibraries();
        new import_obsidian6.Notice("\u8BCD\u5E93\u5DF2\u5237\u65B0");
        this.renderLibraryTable();
      })();
    });
  }
  /** 渲染「词库列表」二级页面（替换整页内容；返回栏样式与分组二级页一致） */
  renderLibraryListSubpage() {
    const { containerEl } = this;
    containerEl.empty();
    containerEl.addClass("blfc-plugin");
    const nav = containerEl.createDiv({ cls: "blfc-fmt-subnav" });
    const backBtn = nav.createEl("button", { text: "\u8FD4\u56DE", cls: "blfc-fmt-btn" });
    backBtn.addEventListener("click", () => this.backToOverview());
    nav.createSpan({ text: "\u8BCD\u5E93\u7BA1\u7406", cls: "blfc-fmt-subnav-crumb" });
    nav.createSpan({ text: "\u203A \u8BCD\u5E93\u5217\u8868", cls: "blfc-fmt-subnav-crumb" });
    new import_obsidian6.Setting(containerEl).setName("\u8BCD\u5E93\u5217\u8868").setHeading();
    containerEl.createDiv({
      text: "\u70B9\u51FB\u8BCD\u5E93\u884C\u53EF\u5207\u6362\u5F53\u524D\u8BCD\u5E93\uFF1B\u25CF = \u5F53\u524D\u4F7F\u7528\u3002",
      cls: "blfc-fmt-hint"
    });
    this.libraryTableContainer = containerEl.createDiv({ cls: "blfc-lib-table-wrap" });
    this.renderLibraryTable();
  }
  // 渲染词库表格
  renderLibraryTable() {
    if (!this.libraryTableContainer) return;
    this.libraryTableContainer.empty();
    const libraries = this.plugin.libraryManager.getAvailableLibraries();
    const activeLibrary = this.plugin.libraryManager.activeLibrary;
    if (libraries.length === 0) {
      this.libraryTableContainer.createEl("p", {
        text: this.plugin.settings.libraryFolder ? `\u5728 "${this.plugin.settings.libraryFolder}" \u4E2D\u6CA1\u6709\u627E\u5230\u8BCD\u5E93\u6587\u4EF6` : "\u8BF7\u5148\u8BBE\u7F6E\u8BCD\u5E93\u6587\u4EF6\u5939",
        cls: "blfc-lib-empty"
      });
      return;
    }
    const table = this.libraryTableContainer.createEl("table", { cls: "blfc-lib-table" });
    const thead = table.createEl("thead");
    const headRow = thead.createEl("tr");
    ["\u8BCD\u5E93", "\u8BCD\u6761", "\u989C\u8272", "\u56FE\u6807", "\u5F53\u524D"].forEach((h, i) => {
      const th = headRow.createEl("th", { text: h });
      if (i === 1) th.addClass("blfc-col-num");
      else if (i === 2 || i === 4) th.addClass("blfc-col-center");
    });
    const tbody = table.createEl("tbody");
    libraries.forEach((libraryName) => {
      var _a;
      const info = this.plugin.libraryManager.getLibraryInfo(libraryName);
      const isActive = activeLibrary === libraryName;
      const tr = tbody.createEl("tr", { cls: "blfc-lib-row" });
      if (isActive) tr.addClass("blfc-row-active");
      tr.addEventListener("click", () => {
        void (async () => {
          const ok = await this.plugin.libraryManager.setActiveLibrary(libraryName);
          if (!ok) return;
          await this.plugin.buildSmartCompletionIndex();
          this.plugin.updateStatusBar();
          if (this.plugin.quickPanel) this.plugin.quickPanel.refresh();
          this.renderLibraryTable();
        })();
      });
      tr.createEl("td", { text: libraryName, cls: "blfc-col-name" });
      tr.createEl("td", { text: info ? String(info.itemCount) : "0", cls: "blfc-col-num" });
      const tdColor = tr.createEl("td", { cls: "blfc-col-center" });
      const colorInput = tdColor.createEl("input", {
        type: "color",
        cls: "blfc-lib-color"
      });
      const currentColor = this.plugin.settings.libraryColors[libraryName] || DEFAULT_LIBRARY_PALETTE[libraries.indexOf(libraryName) % DEFAULT_LIBRARY_PALETTE.length];
      colorInput.value = currentColor;
      colorInput.addEventListener("click", (e) => e.stopPropagation());
      colorInput.addEventListener("input", () => {
        this.plugin.settings.libraryColors[libraryName] = colorInput.value;
        void this.plugin.saveSettings();
        if (this.plugin.quickPanel) this.plugin.quickPanel.refresh();
      });
      const tdIcon = tr.createEl("td");
      const iconInput = tdIcon.createEl("input", {
        type: "text",
        cls: "blfc-lib-icon",
        placeholder: "\u56FE\u6807\u540D / Emoji / <svg>"
      });
      iconInput.value = ((_a = this.plugin.settings.libraryIcons) == null ? void 0 : _a[libraryName]) || "";
      iconInput.addEventListener("click", (e) => e.stopPropagation());
      iconInput.addEventListener("input", () => {
        const v = iconInput.value.trim();
        if (v) {
          this.plugin.settings.libraryIcons[libraryName] = v;
        } else {
          delete this.plugin.settings.libraryIcons[libraryName];
        }
        void this.plugin.saveSettings();
        if (this.plugin.quickPanel) this.plugin.quickPanel.refresh();
      });
      iconInput.addEventListener("blur", () => {
        const v = iconInput.value.trim();
        if (!v || !/^[a-z0-9][a-z0-9-]*$/i.test(v)) {
          iconInput.classList.remove("blfc-lib-icon-err");
          iconInput.title = "";
          return;
        }
        const probe = document.body.createSpan();
        try {
          (0, import_obsidian6.setIcon)(probe, v);
        } catch (e) {
        }
        const ok = !!probe.querySelector("svg");
        probe.remove();
        if (ok) {
          iconInput.classList.remove("blfc-lib-icon-err");
          iconInput.title = "";
        } else {
          iconInput.classList.add("blfc-lib-icon-err");
          iconInput.title = "\u65E0\u6CD5\u89E3\u6790\u6B64\u56FE\u6807\u540D\uFF1A\u8BF7\u786E\u8BA4\u6765\u6E90\u63D2\u4EF6\u5DF2\u542F\u7528\uFF08CI-* \u9700 Custom Icons \u63D2\u4EF6\uFF09\uFF0C\u6216\u6539\u7528 lucide \u5185\u7F6E\u56FE\u6807\u540D / Emoji";
        }
      });
      const tdCurrent = tr.createEl("td", { cls: "blfc-col-center" });
      tdCurrent.createSpan({
        text: isActive ? "\u25CF" : "\u25CB",
        cls: isActive ? "blfc-dot-on" : "blfc-dot-off"
      });
    });
  }
};

// src/main.ts
var CJK_BASIC_RE = /[\u4e00-\u9fff]/;
var pinyinCache = /* @__PURE__ */ new Map();
var pinyinCollator;
function getPinyinCollator() {
  if (pinyinCollator !== void 0) return pinyinCollator;
  try {
    const col = new Intl.Collator("zh-Hans-CN-u-co-pinyin");
    const sortedByPinyin = col.compare("\u554A", "\u516B") < 0 && col.compare("\u516B", "\u531D") < 0;
    pinyinCollator = sortedByPinyin ? col : null;
  } catch (e) {
    pinyinCollator = null;
  }
  return pinyinCollator;
}
var SimpleScriptCompleter = class extends import_obsidian7.Plugin {
  constructor() {
    super(...arguments);
    this.settings = { ...DEFAULT_SETTINGS };
    this.sceneNumberGenerator = new SceneNumberGenerator();
    this.quickPanel = null;
    // 智能补全索引
    this.allSmartItems = [];
    this.prefixIndex = /* @__PURE__ */ new Map();
    // 词库文件 modify 事件防抖器（编辑词库时避免逐键重建索引）
    this._debouncedFileChange = null;
    // 使用频次持久化防抖（选中补全后累计 usageStats，避免每次选中都写盘）
    this._debouncedSaveSettings = null;
  }
  async onload() {
    await this.loadSettings();
    this.addSettingTab(new SimpleScriptSettingTab(this.app, this));
    this.libraryManager = new LibraryManager(this);
    await this.libraryManager.initialize();
    this.formatsManager = new FormatsManager(this);
    await this.formatsManager.initialize();
    await this.buildSmartCompletionIndex();
    this.updateStatusBar();
    this.globalListener = new GlobalInputListener(this);
    this.globalListener.activate();
    this.addCommands();
    this.sceneNumberGenerator = new SceneNumberGenerator();
    this.setupStatusBar();
    if (this.settings.enableQuickPanel) {
      this.quickPanel = new LibraryEdgeStrips(this);
      this.quickPanel.create();
    }
    this.registerEvent(
      this.app.workspace.on("file-open", async (file) => {
        if (file) {
          await this.buildSmartCompletionIndex();
          this.updateStatusBar();
          if (this.quickPanel) this.quickPanel.refresh();
        }
      })
    );
    if (!this._debouncedFileChange) {
      this._debouncedFileChange = (0, import_obsidian7.debounce)(
        (file) => {
          void this.handleFileChange(file);
        },
        400,
        true
      );
    }
    this.registerEvent(this.app.vault.on("create", (file) => this.handleFileChange(file)));
    this.registerEvent(this.app.vault.on("delete", (file) => this.handleFileChange(file)));
    this.registerEvent(this.app.vault.on("rename", (file) => this.handleFileChange(file)));
    this.registerEvent(this.app.vault.on("modify", (file) => this._debouncedFileChange(file)));
  }
  onunload() {
    if (this.globalListener) this.globalListener.deactivate();
    if (this.quickPanel) {
      this.quickPanel.destroy();
      this.quickPanel = null;
    }
  }
  async handleFileChange(file) {
    if (!this.settings.enableAutoRefresh) return;
    if (!(file instanceof import_obsidian7.TFile)) return;
    const libraryDir = this.libraryManager.getLibraryDirectory();
    if (libraryDir && file.path.startsWith(libraryDir) && file.extension === "md") {
      const changedLibraryName = file.basename;
      const isActiveLibrary = this.libraryManager.activeLibrary === changedLibraryName;
      await this.libraryManager.reloadLibraries();
      if (isActiveLibrary) {
        await this.buildSmartCompletionIndex();
        this.updateStatusBar();
        if (this.quickPanel) this.quickPanel.refresh();
        if (this.settings.showAutoRefreshNotice) {
          new import_obsidian7.Notice(`"${changedLibraryName}" \u8BCD\u5E93\u5DF2\u81EA\u52A8\u5237\u65B0`);
        }
      }
      if (this.quickPanel) this.quickPanel.refresh();
    }
  }
  async loadSettings() {
    const defaults = Object.assign({}, DEFAULT_SETTINGS);
    const saved = await this.loadData();
    this.settings = { ...defaults, ...saved != null ? saved : {} };
  }
  async saveSettings() {
    await this.saveData(this.settings);
    if (this.settings.enableQuickPanel && !this.quickPanel) {
      this.quickPanel = new LibraryEdgeStrips(this);
      this.quickPanel.create();
    } else if (!this.settings.enableQuickPanel && this.quickPanel) {
      this.quickPanel.destroy();
      this.quickPanel = null;
    }
  }
  /** 选中补全项时累计使用频次（MRU 排序信号），防抖写盘避免每次选中都落盘 */
  recordUsage(s) {
    const display = s.display || s.name;
    if (!display) return;
    const library = s.library || this.libraryManager.activeLibrary || "";
    const key = `${library}::${display}`;
    const stats = this.settings.usageStats || (this.settings.usageStats = {});
    stats[key] = (stats[key] || 0) + 1;
    if (!this._debouncedSaveSettings) {
      this._debouncedSaveSettings = (0, import_obsidian7.debounce)(() => {
        void this.saveSettings();
      }, 800, false);
    }
    this._debouncedSaveSettings();
  }
  /** 取某条候选的累计使用次数（MRU 排序用） */
  getUsage(item) {
    const stats = this.settings.usageStats;
    if (!stats) return 0;
    return stats[`${item.library}::${item.display}`] || 0;
  }
  // ============ 状态栏 ============
  setupStatusBar() {
    this.statusBarItem = this.addStatusBarItem();
    this.updateStatusBar();
  }
  updateStatusBar() {
    if (!this.statusBarItem) return;
    const activeLibrary = this.libraryManager.activeLibrary;
    const libraryDir = this.libraryManager.getLibraryDirectory();
    if (activeLibrary && libraryDir) {
      const info = this.libraryManager.getLibraryInfo(activeLibrary);
      const itemCount = info ? info.itemCount : 0;
      this.statusBarItem.setText(`\u{1F4DA} ${activeLibrary}(${itemCount})`);
      this.statusBarItem.onClick = () => this.showLibrarySwitcher();
    } else if (libraryDir) {
      this.statusBarItem.setText("\u{1F4DA} \u65E0\u8BCD\u5E93");
      this.statusBarItem.onClick = () => this.showLibrarySwitcher();
    } else {
      this.statusBarItem.setText("\u{1F4DA} \u672A\u8BBE\u7F6E");
      this.statusBarItem.onClick = () => {
        this.openSettings();
      };
    }
  }
  /** 打开插件设置页 */
  openSettings() {
    const setting = this.app.setting;
    if (setting) {
      setting.open();
      setting.openTabById(this.manifest.id);
    }
  }
  // ============ 智能补全索引 ============
  async buildSmartCompletionIndex() {
    this.allSmartItems = [];
    const activeData = this.libraryManager.getActiveLibraryData();
    if (activeData && activeData.name) {
      for (const [category, items] of Object.entries(activeData)) {
        if (category === "name" || category === "metadata") continue;
        const categoryType = CATEGORY_TYPE_MAP[category] || category;
        const categoryDisplay = CATEGORY_DISPLAY_MAP[category] || category.replace(/_/g, " ");
        if (Array.isArray(items) && items.length > 0) {
          for (const rawItem of items) {
            const item = rawItem;
            const displayText = item.display || String(rawItem);
            const insertText = item.insert || displayText;
            this.allSmartItems.push({
              display: displayText,
              insert: insertText,
              type: categoryType,
              typeDisplay: categoryDisplay,
              source: "library",
              library: activeData.name,
              description: item.description || ""
            });
          }
        }
      }
      if (this.settings.enableCombos) {
        const scenes = this.getCategoryItems(activeData, ["scenes", "\u573A\u666F"]);
        const times = this.getCategoryItems(activeData, ["times", "\u65F6\u95F4\u6BB5"]);
        if (scenes.length > 0 && times.length > 0) {
          this.buildSceneTimeCombinations(
            activeData.name,
            scenes,
            this.capComboDimension(times, scenes.length)
          );
        }
        const characters = this.getCategoryItems(activeData, ["characters", "\u89D2\u8272"]);
        const dialogues = this.getCategoryItems(activeData, ["dialogues", "\u53F0\u8BCD"]);
        if (characters.length > 0 && dialogues.length > 0) {
          this.buildCharacterDialogueCombinations(
            activeData.name,
            characters,
            this.capComboDimension(dialogues, characters.length)
          );
        }
      }
    }
    if (this.settings.enablePinyin) {
      for (const item of this.allSmartItems) {
        item.py = this.getPinyinFirstLetters(item.display);
      }
    }
    this.buildPrefixIndex();
  }
  getCategoryItems(data, keys) {
    for (const key of keys) {
      const value = data[key];
      if (Array.isArray(value)) return value;
    }
    return [];
  }
  /**
   * 组合项笛卡尔积上限：把「另一维度」按 comboMaxItems 截断，
   * 使 场景数×时间数 ≤ comboMaxItems，避免大词库下组合项淹没真实词条。
   */
  capComboDimension(dimension, otherLen) {
    const max = Math.max(1, this.settings.comboMaxItems || 60);
    if (otherLen <= 0) return dimension;
    const allowed = Math.floor(max / otherLen);
    if (allowed >= dimension.length) return dimension;
    return dimension.slice(0, Math.max(1, allowed));
  }
  buildSceneTimeCombinations(libraryName, scenes, times) {
    for (const scene of scenes) {
      for (const time of times) {
        const sceneDisplay = scene.display || String(scene);
        const sceneInsert = scene.insert || sceneDisplay;
        const timeDisplay = time.display || String(time);
        const timeInsert = time.insert || timeDisplay;
        this.allSmartItems.push({
          display: `${sceneDisplay} - ${timeDisplay}`,
          insert: `${sceneInsert} - ${timeInsert}`,
          type: "scene_time_combo",
          typeDisplay: "\u573A\u666F+\u65F6\u95F4",
          source: "combo",
          scene: sceneDisplay,
          time: timeDisplay,
          library: libraryName
        });
      }
    }
  }
  buildCharacterDialogueCombinations(libraryName, characters, dialogues) {
    if (characters.length === 0 || dialogues.length === 0) return;
    characters.slice(0, 5).forEach((character) => {
      dialogues.slice(0, 3).forEach((dialogue) => {
        const charDisplay = character.display || String(character);
        const charInsert = character.insert || charDisplay;
        const dialogueDisplay = dialogue.display || String(dialogue);
        const dialogueInsert = dialogue.insert || dialogueDisplay;
        this.allSmartItems.push({
          display: `${charDisplay}: ${dialogueDisplay}`,
          insert: `${charInsert}: ${dialogueInsert}`,
          type: "dialogue_combo",
          typeDisplay: "\u89D2\u8272\u5BF9\u8BDD",
          source: "combo",
          character: charDisplay,
          dialogue: dialogueDisplay,
          library: libraryName
        });
      });
    });
  }
  /**
   * 构建前缀索引。查询词始终是「当前输入词」的【后缀】（resolveCompletion 从词尾回退试探），
   * 因此同时建「前缀索引」与「后缀索引」才能让补全查询真正命中桶、把查询复杂度从 O(N) 降到 O(桶)。
   * 索引键长度封顶到 MAX_COMPLETION_QUERY_LEN：更长的查询极少出现，且会退化为全表扫描保召回。
   */
  buildPrefixIndex() {
    const raw = /* @__PURE__ */ new Map();
    const addKey = (key, item) => {
      let set = raw.get(key);
      if (!set) {
        set = /* @__PURE__ */ new Set();
        raw.set(key, set);
      }
      set.add(item);
    };
    const cap = MAX_COMPLETION_QUERY_LEN;
    for (const item of this.allSmartItems) {
      const text = item.display;
      const lower = text.toLowerCase();
      addKey(lower, item);
      for (let i = 0; i < text.length; i++) {
        addKey(text[i].toLowerCase(), item);
      }
      const preLen = Math.min(cap, text.length);
      for (let i = 1; i <= preLen; i++) {
        addKey(lower.substring(0, i), item);
      }
      const sufStart = Math.max(0, text.length - cap);
      for (let i = sufStart; i < text.length; i++) {
        addKey(lower.substring(i), item);
      }
      if (this.settings.enablePinyin && item.py) {
        const py = item.py;
        addKey(py, item);
        const pyPre = Math.min(cap, py.length);
        for (let i = 1; i <= pyPre; i++) addKey(py.substring(0, i), item);
        const pySuf = Math.max(0, py.length - cap);
        for (let i = pySuf; i < py.length; i++) addKey(py.substring(i), item);
      }
    }
    this.prefixIndex = /* @__PURE__ */ new Map();
    for (const [key, set] of raw) {
      this.prefixIndex.set(key, [...set]);
    }
  }
  getPinyinFirstLetter(char) {
    if (!char) return "";
    if (/[a-zA-Z]/.test(char)) return char.toLowerCase();
    if (!CJK_BASIC_RE.test(char)) return "";
    const cached = pinyinCache.get(char);
    if (cached !== void 0) return cached;
    let result = "";
    const col = getPinyinCollator();
    if (col) {
      let lo = 0;
      let hi = PINYIN_BRANCH.length - 1;
      while (lo <= hi) {
        const mid = lo + hi >> 1;
        if (col.compare(char, PINYIN_BRANCH[mid][1]) >= 0) {
          result = PINYIN_BRANCH[mid][0];
          lo = mid + 1;
        } else {
          hi = mid - 1;
        }
      }
    }
    pinyinCache.set(char, result);
    return result;
  }
  getPinyinFirstLetters(text) {
    let result = "";
    for (const char of text) {
      const pinyin = this.getPinyinFirstLetter(char);
      if (pinyin) result += pinyin;
    }
    return result;
  }
  // ============ 智能补全建议 ============
  /**
   * 按上下文类别过滤候选。
   * - enableContextAware 关闭 → 不过滤（返回全部，兼容旧行为）。
   * - 未知上下文 → 回退到 general 宽泛列表（不再是 'all' 全放行，避免正文行下感知形同虚设）。
   * - 自定义类别（不在 SYSTEM_CATEGORY_SET）始终透传，不受上下文收窄影响。
   */
  getSmartSuggestionsForContext(contextType, query = "") {
    const q = query ? query.trim() : "";
    const qLower = q.toLowerCase();
    const filterByContext = this.settings.enableContextAware;
    const ctxCats = filterByContext ? CONTEXT_CATEGORY_MAP[contextType] || CONTEXT_CATEGORY_MAP["general"] : null;
    let effectiveCats = ctxCats;
    if (filterByContext && contextType === "character_name" && this.settings.autoDialogue) {
      effectiveCats = ctxCats ? [...ctxCats, "dialogue"] : ["dialogue"];
    }
    const contextOk = (item) => !effectiveCats || effectiveCats.includes(item.type) || !SYSTEM_CATEGORY_SET.has(item.type);
    if (!qLower) {
      return this.sortSuggestions(
        this.allSmartItems.filter(contextOk),
        q
      ).slice(0, this.settings.smartMaxSuggestions || 10);
    }
    const bucket = this.prefixIndex.get(qLower);
    const source = bucket && bucket.length ? bucket : this.allSmartItems;
    const matched = source.filter(
      (item) => contextOk(item) && this.itemMatchesQuery(item, qLower)
    );
    if (matched.length === 0 && qLower.length >= 2) {
      const fuzzy = this.allSmartItems.filter(
        (item) => contextOk(item) && this.fuzzyMatch(item.display, qLower)
      );
      return this.sortSuggestions(fuzzy, q).slice(
        0,
        this.settings.smartMaxSuggestions || 10
      );
    }
    return this.sortSuggestions(matched, q).slice(0, this.settings.smartMaxSuggestions || 10);
  }
  /**
   * 子序列模糊匹配：query 的每个字符按顺序在目标文本中出现即命中。
   * 比编辑距离更宽容（可容忍插入/漏打），且实现为 O(target) 单趟扫描，代价可忽略。
   * 仅作兜底，不参与排序权重，避免干扰精确/前缀的正常排序。
   */
  fuzzyMatch(target, queryLower) {
    const hay = target.toLowerCase();
    if (!hay) return false;
    let ti = 0;
    for (let qi = 0; qi < queryLower.length; qi++) {
      const c = queryLower[qi];
      const found = hay.indexOf(c, ti);
      if (found === -1) return false;
      ti = found + 1;
    }
    return true;
  }
  /** 单条候选是否命中查询词（显示文本 / 拼音首字母 / 描述三路匹配） */
  itemMatchesQuery(item, queryLower) {
    var _a;
    if (item.display.toLowerCase().includes(queryLower)) return true;
    if (this.settings.enablePinyin) {
      const py = (_a = item.py) != null ? _a : this.getPinyinFirstLetters(item.display);
      if (py && py.includes(queryLower)) return true;
    }
    if (item.description && item.description.toLowerCase().includes(queryLower)) return true;
    return false;
  }
  /**
   * 智能补全入口：从光标前文本解析出最佳查询片段与对应候选。
   *
   * 旧实现只取光标前最后 1 个字符作为查询词，带来两个问题：
   *  1. 中文多字输入时替换长度不足 —— 输入「主角」再选候选会得到「主主角」；
   *  2. 光标前是空白或标点时，查询词退化为空串，弹出一堆无关候选。
   *
   * 现在的策略：先截取「当前输入词」（遇空白/标点即停，保证不跨既有正文），
   * 再从长到短回退试探，取第一个能命中候选的长度。
   * 返回的 query 同时决定了插入时的替换长度，两者严格一致。
   */
  resolveCompletion(textBefore, contextType) {
    const minLen = Math.max(1, this.settings.smartMinLength || 1);
    const raw = textBefore.slice(this.findCompletionWordStart(textBefore));
    if (raw.length < minLen) return null;
    const maxLen = Math.min(raw.length, MAX_COMPLETION_QUERY_LEN);
    for (let len = maxLen; len >= minLen; len--) {
      const query = raw.slice(raw.length - len);
      const suggestions = this.getSmartSuggestionsForContext(contextType, query);
      if (suggestions.length > 0) return { query, suggestions };
    }
    return null;
  }
  /** 定位「当前输入词」的起始下标：遇空白或中英文标点即停 */
  findCompletionWordStart(textBefore) {
    for (let i = textBefore.length - 1; i >= 0; i--) {
      if (WORD_BOUNDARY_RE.test(textBefore[i])) return i + 1;
    }
    return 0;
  }
  sortSuggestions(suggestions, query) {
    if (!query || !query.trim()) {
      return suggestions.slice().sort((a, b) => {
        const freqDiff = this.getUsage(b) - this.getUsage(a);
        if (freqDiff !== 0) return freqDiff;
        if (a.library === this.libraryManager.activeLibrary && b.library !== this.libraryManager.activeLibrary)
          return -1;
        if (a.library !== this.libraryManager.activeLibrary && b.library === this.libraryManager.activeLibrary)
          return 1;
        if (a.source === "library" && b.source === "default") return -1;
        if (a.source === "default" && b.source === "library") return 1;
        const aOrder = TYPE_ORDER[a.type] || 99;
        const bOrder = TYPE_ORDER[b.type] || 99;
        if (aOrder !== bOrder) return aOrder - bOrder;
        return a.display.localeCompare(b.display);
      });
    }
    const queryLower = query.toLowerCase();
    return suggestions.slice().sort((a, b) => {
      if (a.display.toLowerCase() === queryLower && b.display.toLowerCase() !== queryLower)
        return -1;
      if (a.display.toLowerCase() !== queryLower && b.display.toLowerCase() === queryLower)
        return 1;
      const aStartsWith = a.display.toLowerCase().startsWith(queryLower);
      const bStartsWith = b.display.toLowerCase().startsWith(queryLower);
      if (aStartsWith && !bStartsWith) return -1;
      if (!aStartsWith && bStartsWith) return 1;
      const freqDiff = this.getUsage(b) - this.getUsage(a);
      if (freqDiff !== 0) return freqDiff;
      if (a.library === this.libraryManager.activeLibrary && b.library !== this.libraryManager.activeLibrary)
        return -1;
      if (a.library !== this.libraryManager.activeLibrary && b.library === this.libraryManager.activeLibrary)
        return 1;
      if (a.source === "library" && b.source === "default") return -1;
      if (a.source === "default" && b.source === "library") return 1;
      return a.display.length - b.display.length;
    });
  }
  // ============ 命令注册 ============
  addCommands() {
    this.addCommand({
      id: "insert-dialogue",
      name: "\u63D2\u5165\u89D2\u8272\u5BF9\u8BDD",
      callback: () => {
        const el = document.activeElement;
        if (el && TextInserter.isEditable(el)) {
          TextInserter.insertText(el, "\u5BF9\u8BDD\u5185\u5BB9\n");
        }
      }
    });
    this.addCommand({
      id: "open-template-menu",
      name: "\u6253\u5F00\u6A21\u677F\u83DC\u5355",
      callback: () => this.runTemplateMenu()
    });
    for (let slot = 1; slot <= MAX_QUICK_COMMANDS; slot++) {
      this.addCommand({
        id: `insert-template-quick-${slot}`,
        name: `\u63D2\u5165\u6A21\u677F \xB7 \u76F4\u8FBE ${slot}`,
        callback: () => this.runQuickCommand(slot)
      });
    }
    this.addCommand({
      id: "renumber-current-episode",
      name: "\u91CD\u65B0\u7F16\u53F7\u5F53\u524D\u96C6\u573A\u666F",
      callback: () => {
        const el = document.activeElement;
        const cm = el ? TextInserter.getCodeMirrorView(el) : null;
        if (cm) {
          const proxy = TextInserter.createCMEditorProxy(cm);
          void this.renumberScenesInCurrentEpisode(proxy);
        } else {
          new import_obsidian7.Notice("\u573A\u666F\u91CD\u7F16\u53F7\u4EC5\u652F\u6301 Obsidian \u7F16\u8F91\u5668");
        }
      }
    });
    this.addCommand({
      id: "renumber-all-scenes",
      name: "\u91CD\u65B0\u7F16\u53F7\u6574\u4E2A\u6587\u6863",
      callback: () => {
        const el = document.activeElement;
        const cm = el ? TextInserter.getCodeMirrorView(el) : null;
        if (cm) {
          const proxy = TextInserter.createCMEditorProxy(cm);
          void this.renumberAllScenes(proxy);
        } else {
          new import_obsidian7.Notice("\u573A\u666F\u91CD\u7F16\u53F7\u4EC5\u652F\u6301 Obsidian \u7F16\u8F91\u5668");
        }
      }
    });
    this.addCommand({
      id: "switch-library",
      name: "\u5207\u6362\u8BCD\u5E93",
      callback: () => {
        void this.showLibrarySwitcher();
      }
    });
    this.addCommand({
      id: "reload-libraries",
      name: "\u91CD\u65B0\u52A0\u8F7D\u8BCD\u5E93",
      callback: async () => {
        await this.libraryManager.reloadLibraries();
        const libraries = this.libraryManager.getAvailableLibraries();
        if (libraries.length === 0) {
          new import_obsidian7.Notice("\u6CA1\u6709\u627E\u5230\u8BCD\u5E93\uFF0C\u8BF7\u5148\u5728\u8BBE\u7F6E\u4E2D\u6307\u5B9A\u8BCD\u5E93\u6587\u4EF6\u5939");
          return;
        }
        await this.buildSmartCompletionIndex();
        this.updateStatusBar();
        if (this.quickPanel) this.quickPanel.refresh();
        const active = this.libraryManager.activeLibrary;
        new import_obsidian7.Notice(
          active ? `\u8BCD\u5E93\u5DF2\u91CD\u65B0\u52A0\u8F7D\uFF08${libraries.length} \u4E2A\uFF09\uFF0C\u5F53\u524D\uFF1A${active}` : `\u8BCD\u5E93\u5DF2\u91CD\u65B0\u52A0\u8F7D\uFF08${libraries.length} \u4E2A\uFF09\uFF0C\u5F53\u524D\u672A\u9009\u4E2D`
        );
      }
    });
    this.addCommand({
      id: "open-library-folder",
      name: "\u6253\u5F00\u8BCD\u5E93\u6587\u4EF6\u5939",
      callback: async () => {
        const libraryDir = this.libraryManager.getLibraryDirectory();
        if (libraryDir) {
          const folder = this.app.vault.getAbstractFileByPath(libraryDir);
          if (folder) {
            const explorerLeaves = this.app.workspace.getLeavesOfType("file-explorer");
            if (explorerLeaves.length > 0) {
              const explorer = explorerLeaves[0].view;
              if (explorer.revealInFolder) explorer.revealInFolder(folder);
              await this.app.workspace.revealLeaf(explorerLeaves[0]);
            } else {
              new import_obsidian7.Notice(`\u8BCD\u5E93\u6587\u4EF6\u5939: ${libraryDir}`);
            }
          } else {
            new import_obsidian7.Notice(`\u8BCD\u5E93\u6587\u4EF6\u5939\u4E0D\u5B58\u5728: ${libraryDir}`);
          }
        } else {
          new import_obsidian7.Notice("\u8BF7\u5148\u8BBE\u7F6E\u8BCD\u5E93\u6587\u4EF6\u5939");
        }
      }
    });
    this.addCommand({
      id: "create-library-file",
      name: "\u521B\u5EFA\u65B0\u8BCD\u5E93\u6587\u4EF6",
      callback: () => {
        const libraryDir = this.libraryManager.getLibraryDirectory();
        if (libraryDir) {
          const createLibraryFile = async (libraryName) => {
            if (libraryName && libraryName.trim()) {
              let fileName = `${libraryName.trim()}.md`;
              let filePath = `${libraryDir}/${fileName}`;
              let counter = 1;
              while (this.app.vault.getAbstractFileByPath(filePath)) {
                fileName = `${libraryName.trim()}_${counter}.md`;
                filePath = `${libraryDir}/${fileName}`;
                counter++;
              }
              const template = `# ${libraryName.trim()}

## \u89D2\u8272
\u4E3B\u89D2
\u914D\u89D2

## \u573A\u666F
\u4E3B\u8981\u573A\u666F1
\u4E3B\u8981\u573A\u666F2

## \u65F6\u95F4\u6BB5
\u767D\u5929
\u591C\u665A

## \u52A8\u4F5C
\u5E38\u7528\u52A8\u4F5C1
\u5E38\u7528\u52A8\u4F5C2

## \u53F0\u8BCD
\u5E38\u7528\u53F0\u8BCD1
\u5E38\u7528\u53F0\u8BCD2`;
              try {
                await this.app.vault.create(filePath, template);
                new import_obsidian7.Notice(`\u5DF2\u521B\u5EFA\u8BCD\u5E93\u6587\u4EF6: ${fileName}`);
                await this.libraryManager.reloadLibraries();
                await this.libraryManager.setActiveLibrary(libraryName.trim());
                await this.buildSmartCompletionIndex();
                this.updateStatusBar();
                if (this.quickPanel) this.quickPanel.refresh();
              } catch (e) {
                console.error("\u521B\u5EFA\u8BCD\u5E93\u6587\u4EF6\u5931\u8D25:", e);
                new import_obsidian7.Notice("\u521B\u5EFA\u8BCD\u5E93\u6587\u4EF6\u5931\u8D25");
              }
            }
          };
          const modal = new LibraryCreationModal(this.app, (name) => {
            void createLibraryFile(name);
          });
          modal.open();
        } else {
          new import_obsidian7.Notice("\u8BF7\u5148\u8BBE\u7F6E\u8BCD\u5E93\u6587\u4EF6\u5939");
        }
      }
    });
    this.addCommand({
      id: "toggle-quick-panel",
      name: "\u6253\u5F00\u8BCD\u5E93\u9009\u62E9\u5668",
      callback: () => {
        void this.showLibrarySwitcher();
      }
    });
    this.addCommand({
      id: "debug-index",
      name: "\u8C03\u8BD5\u7D22\u5F15\u72B6\u6001",
      callback: () => {
        const activeData = this.libraryManager.getActiveLibraryData();
        const allLibraries = this.libraryManager.getAvailableLibraries();
        const itemCount = this.allSmartItems ? this.allSmartItems.length : 0;
        let message = `\u7D22\u5F15\u72B6\u6001:
`;
        message += `\u603B\u8BCD\u6761\u6570: ${itemCount}
`;
        message += `\u5F53\u524D\u8BCD\u5E93: ${this.libraryManager.activeLibrary || "\u65E0"}
`;
        message += `\u53EF\u7528\u8BCD\u5E93: ${allLibraries.length} \u4E2A\uFF08${allLibraries.join("\u3001") || "\u65E0"}\uFF09
`;
        message += `\u8BCD\u5E93\u6587\u4EF6\u5939: ${this.libraryManager.getLibraryDirectory() || "\u672A\u8BBE\u7F6E"}`;
        if (activeData) {
          let typed = 0;
          let untyped = 0;
          const catLines = [];
          for (const [category, items] of Object.entries(activeData)) {
            if (category === "name" || category === "metadata" || !Array.isArray(items)) continue;
            const type = CATEGORY_TYPE_MAP[category] || category;
            const known = SYSTEM_CATEGORY_SET.has(type);
            if (known) typed += items.length;
            else untyped += items.length;
            catLines.push(
              `${CATEGORY_DISPLAY_MAP[type] || category}: ${items.length}\u4E2A${known ? "" : "\uFF08\u81EA\u5B9A\u4E49\uFF09"}`
            );
          }
          message += `

\u8BCD\u5E93\u7C7B\u522B\u7EDF\u8BA1:
${catLines.join("\n")}`;
          if (untyped > 0) {
            message += `

\u26A0 ${untyped} \u6761\uFF08${Math.round(untyped / (typed + untyped) * 100)}%\uFF09\u672A\u5339\u914D\u5230\u5DF2\u77E5\u7C7B\u522B\uFF0C\u4E0D\u53C2\u4E0E\u4E0A\u4E0B\u6587\u8FC7\u6EE4\u4E0E\u7C7B\u578B\u6392\u5E8F\u3002`;
          }
        }
        new import_obsidian7.Notice(message);
      }
    });
    this.addCommand({
      id: "insert-timestamp",
      name: "\u63D2\u5165\u65F6\u95F4\u6233 (YYYY-MM-DD-HH:mm)",
      callback: () => this.insertTimestampAtCursor()
    });
    this.addCommand({
      id: "insert-timestamp-frontmatter",
      name: "\u5199\u5165\u65F6\u95F4\u6233\u5230 frontmatter",
      callback: async () => {
        const file = this.app.workspace.getActiveFile();
        if (!file || !(file instanceof import_obsidian7.TFile)) {
          new import_obsidian7.Notice("\u6CA1\u6709\u6253\u5F00\u7684\u7B14\u8BB0\u6587\u4EF6");
          return;
        }
        const ts = this.formatTimestamp(/* @__PURE__ */ new Date());
        try {
          await this.app.fileManager.processFrontMatter(file, (fm) => {
            const data = fm;
            if (!data.created) data.created = ts;
            data.updated = ts;
          });
          new import_obsidian7.Notice(`\u5DF2\u5199\u5165\u65F6\u95F4\u6233: ${ts}`);
        } catch (e) {
          console.error("\u5199\u5165 frontmatter \u65F6\u95F4\u6233\u5931\u8D25:", e);
          new import_obsidian7.Notice("\u5199\u5165\u65F6\u95F4\u6233\u5931\u8D25");
        }
      }
    });
    this.addCommand({
      id: "create-timestamp-note",
      name: "\u7528\u65F6\u95F4\u6233\u521B\u5EFA\u65B0\u7B14\u8BB0",
      callback: async () => {
        const now = /* @__PURE__ */ new Date();
        const ts = this.formatTimestamp(now);
        const name = this.formatTimestampForFilename(now);
        const activeFile = this.app.workspace.getActiveFile();
        const parent = this.app.fileManager.getNewFileParent(
          activeFile ? activeFile.path : ""
        );
        let fileName = `${name}.md`;
        let filePath = parent ? `${parent.path}/${fileName}` : fileName;
        let counter = 1;
        while (this.app.vault.getAbstractFileByPath(filePath)) {
          fileName = `${name}_${counter}.md`;
          filePath = parent ? `${parent.path}/${fileName}` : fileName;
          counter++;
        }
        try {
          const file = await this.app.vault.create(
            filePath,
            `---
created: ${ts}
updated: ${ts}
---

`
          );
          await this.app.workspace.getLeaf(false).openFile(file);
          new import_obsidian7.Notice(`\u5DF2\u521B\u5EFA\u7B14\u8BB0: ${fileName}`);
        } catch (e) {
          console.error("\u521B\u5EFA\u65F6\u95F4\u6233\u7B14\u8BB0\u5931\u8D25:", e);
          new import_obsidian7.Notice("\u521B\u5EFA\u7B14\u8BB0\u5931\u8D25");
        }
      }
    });
  }
  // ============ 时间戳 ============
  formatTimestamp(date = /* @__PURE__ */ new Date()) {
    const pad2 = (n) => String(n).padStart(2, "0");
    return `${date.getFullYear()}-${pad2(date.getMonth() + 1)}-${pad2(date.getDate())}-${pad2(date.getHours())}:${pad2(date.getMinutes())}`;
  }
  formatTimestampForFilename(date = /* @__PURE__ */ new Date()) {
    return this.formatTimestamp(date).replace(/:/g, "");
  }
  insertTimestampAtCursor() {
    const TAG = "[InFlow]";
    const ts = this.formatTimestamp(/* @__PURE__ */ new Date());
    const el = document.activeElement;
    if (el && TextInserter.isEditable(el) && !TextInserter.isInExcludedContainer(el)) {
      TextInserter.insertText(el, ts);
      return;
    }
    const editor = this.app.workspace.activeEditor && this.app.workspace.activeEditor.editor;
    if (editor) {
      editor.replaceSelection(ts);
      return;
    }
    console.warn(`${TAG} \u63D2\u5165\u5931\u8D25\uFF1A\u6CA1\u6709\u53EF\u7528\u76EE\u6807`);
    new import_obsidian7.Notice("\u6CA1\u6709\u627E\u5230\u53EF\u63D2\u5165\u65F6\u95F4\u6233\u7684\u5149\u6807\u4F4D\u7F6E");
  }
  // ============ 词库切换 ============
  async showLibrarySwitcher() {
    const libraries = this.libraryManager.getAvailableLibraries();
    if (libraries.length === 0) {
      new import_obsidian7.Notice("\u6CA1\u6709\u627E\u5230\u8BCD\u5E93\uFF0C\u8BF7\u5148\u8BBE\u7F6E\u8BCD\u5E93\u6587\u4EF6\u5939");
      return;
    }
    const modal = new LibrarySwitcherModal(this.app, libraries, this.libraryManager.activeLibrary, (selectedLibrary) => {
      void this.selectLibrary(selectedLibrary);
    });
    modal.open();
  }
  /** 切库统一流程：置活动词库 → 重建索引 → 刷新状态栏/彩条 → 提示 */
  async selectLibrary(name) {
    if (!name) return;
    await this.libraryManager.setActiveLibrary(name);
    await this.buildSmartCompletionIndex();
    this.updateStatusBar();
    if (this.quickPanel) this.quickPanel.refresh();
    new import_obsidian7.Notice(`\u5DF2\u5207\u6362\u5230\u8BCD\u5E93: ${name}`);
  }
  // ============ 格式模板：统一插入管线 ============
  /** 常驻命令：打开全部模板分组菜单 */
  runTemplateMenu() {
    new FormatSuggestModal(this.app, this, this.formatsManager.file.groups.map((g) => g.id)).open();
  }
  /** 直达命令位：跳转到该位绑定的分组菜单 */
  runQuickCommand(slot) {
    const q = this.formatsManager.quickCommand(slot);
    if (!q || q.groupIds.length === 0) {
      new import_obsidian7.Notice(`\u76F4\u8FBE ${slot} \u672A\u914D\u7F6E\uFF1A\u8BBE\u7F6E \u2192 \u6A21\u677F\u83DC\u5355 \u2192 \u76F4\u8FBE\u547D\u4EE4`);
      return;
    }
    new FormatSuggestModal(this.app, this, q.groupIds).open();
  }
  /**
   * 统一的格式插入管线：模板求值（变量 + $0）→ 删除触发串 → 插入 → 光标落点。
   * editor 非空走 Obsidian Editor（CM）路径；否则走 DOM 元素路径（非 CM 输入框）。
   */
  insertFormatItem(editor, el, s, deleteLen) {
    var _a, _b;
    const template = (_b = (_a = s.template) != null ? _a : s.insert) != null ? _b : "";
    if (!template) return;
    if (editor) {
      const cursor = editor.getCursor();
      const needSceneNumber = /\{episode\}|\{scene\}/.test(template);
      const r = renderFormatTemplate(template, {
        sceneType: this.settings.defaultSceneType,
        getSceneNumber: needSceneNumber ? () => this.sceneNumberGenerator.getNextSceneNumber(editor, cursor) : void 0,
        selection: this.getEditorSelectionSafe(editor)
      });
      const ch = Math.max(0, cursor.ch - deleteLen);
      const base = { line: cursor.line, ch };
      editor.replaceRange(r.text, base, { line: cursor.line, ch: cursor.ch });
      this.placeCursorAfterInsert(editor, base, r);
      return;
    }
    if (el && TextInserter.isEditable(el)) {
      const r = renderFormatTemplate(template, {
        sceneType: this.settings.defaultSceneType
      });
      TextInserter.replaceBeforeCursorSmart(
        el,
        r.text,
        deleteLen,
        r.cursorOffset,
        r.selectFrom,
        r.selectTo
      );
    }
  }
  /** 渲染结果落点：$0 → 光标/选中；无 $0 → 文本尾 */
  placeCursorAfterInsert(editor, base, r) {
    if (r.cursorOffset != null) {
      const rel2 = offsetToLineCh(r.text, r.cursorOffset);
      const pos = {
        line: base.line + rel2.line,
        ch: rel2.line === 0 ? base.ch + rel2.ch : rel2.ch
      };
      if (r.selectFrom != null && r.selectTo != null) {
        const f = offsetToLineCh(r.text, r.selectFrom);
        const t = offsetToLineCh(r.text, r.selectTo);
        editor.setSelection(
          { line: base.line + f.line, ch: f.line === 0 ? base.ch + f.ch : f.ch },
          { line: base.line + t.line, ch: t.line === 0 ? base.ch + t.ch : t.ch }
        );
      } else {
        editor.setCursor(pos);
      }
      return;
    }
    const rel = offsetToLineCh(r.text, r.text.length);
    editor.setCursor({
      line: base.line + rel.line,
      ch: rel.line === 0 ? base.ch + rel.ch : rel.ch
    });
  }
  getEditorSelectionSafe(editor) {
    try {
      return editor.getSelection() || void 0;
    } catch (e) {
      return void 0;
    }
  }
  // ============ 场景编号 ============
  async renumberScenesInCurrentEpisode(editor) {
    const result = this.sceneNumberGenerator.renumberCurrentEpisode(editor);
    if (result.success) {
      new import_obsidian7.Notice(`\u5DF2\u91CD\u65B0\u7F16\u53F7${result.renumberedScenes}\u4E2A\u573A\u666F`);
    } else {
      new import_obsidian7.Notice("\u672A\u627E\u5230\u9700\u8981\u91CD\u65B0\u7F16\u53F7\u7684\u573A\u666F");
    }
  }
  async renumberAllScenes(editor) {
    const result = this.sceneNumberGenerator.renumberAllEpisodes(editor);
    if (result.success) {
      new import_obsidian7.Notice(
        `\u5DF2\u91CD\u65B0\u7F16\u53F7${result.totalRenumbered}\u4E2A\u573A\u666F\uFF0C\u6D89\u53CA${result.episodesRenumbered}\u96C6`
      );
    } else {
      new import_obsidian7.Notice("\u672A\u627E\u5230\u9700\u8981\u91CD\u65B0\u7F16\u53F7\u7684\u573A\u666F");
    }
  }
};
var main_default = SimpleScriptCompleter;
